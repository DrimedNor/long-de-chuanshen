# 后台安全与隐私升级 — 代码交付文档

> **本文档写给执行 AI（豆包）**：请按顺序执行第 1→7 节的改动。每处改动都给出了「旧代码 → 新代码」对照，用旧代码在目标文件中搜索定位，确认上下文一致后整体替换。
> 改动完成后执行第 8 节自测清单，全部通过即完成。
> **不要**改动本任务之外的任何功能；**不要**动 admin.html 里的 `API_BASE` 常量。

---

## 1. 背景与目标

网站（https://longchen-nyingtik.wiki）后端是两个 Cloudflare Worker：

| 文件 | 部署名 | 职责 |
|------|--------|------|
| `stats-auth-worker.js` | longchen-stats-auth（自定义域名 api.longchen-nyingtik.wiki） | 访问统计、密码验证、注册登录、管理后台 API |
| `ai-ask-worker.js` | AI 问答代理（stats.longchen-nyingtik.wiki） | DeepSeek 问答代理 + AI 问答统计 |
| `admin.html` | 构建时复制为 dist/admin/index.html | 管理后台前端 |

本次升级两个目标：

1. **后台认证加固**：废除「明文密码每次请求传输 + 代码硬编码默认密码 admin610」的现状，改为 PBKDF2 哈希验证 + 登录换签名 Token + 失败限速锁定。
2. **数据收集最小隐私化**：IP 只存加盐哈希（不存原文、不可反推）、删除经纬度/邮编等精确定位字段、后台展示层全部脱敏、增加过期数据自动清理。

用户隐私 5 原则（改动验收标准，详见第 9 节对照表）：
1. 不暴露用户信息，不涵盖个人隐私
2. 仅收集设备、IP 地址（脱敏）和用户习惯
3. 数据不与具体人名关联
4. 后台不展示任何个人信息明文
5. 数据即使被黑客获取也无法反推个人

---

## 2. 现状问题诊断（只读了解，不需改）

| # | 问题 | 位置 | 严重度 |
|---|------|------|--------|
| 1 | 管理后台密码**明文**比较，且代码内硬编码默认密码 `admin610`（4 处） | stats-auth-worker.js | 🔴 |
| 2 | 明文密码通过 `X-Admin-Password` 头随每个请求传输；前端 sessionStorage 存明文 | admin.html | 🔴 |
| 3 | 部分接口支持 `?admin=密码` URL 参数传密码（会进访问日志/浏览器历史） | stats-auth-worker.js 4 处 | 🔴 |
| 4 | 访客**原始 IP 落库**（`ip_` 键、device 记录 firstIp、注册用户 ip 字段） | stats-auth-worker.js | 🔴 |
| 5 | IP 记录存**经纬度坐标、邮编、省份**（可精确定位） | stats-auth-worker.js recordDevice | 🔴 |
| 6 | 用户注册密码用 simpleHash（32 位整数哈希，无盐，秒破） | stats-auth-worker.js | 🟡 |
| 7 | admin 接口无失败限速，可无限暴力尝试 | stats-auth-worker.js | 🟡 |
| 8 | 统计数据永久保留，无清理机制 | 两个 Worker | 🟡 |

---

## 3. 改动总览

| 文件 | 改动 |
|------|------|
| `stats-auth-worker.js` | ① 顶部插入安全工具模块（4.1 节整段）② 新增 POST /api/admin/login ③ 新增 verifyAdmin() 并替换 9 处旧密码校验 ④ 删除硬编码 admin610 与 URL 参数传密码 ⑤ CORS 头加 X-Admin-Token ⑥ recordDevice 隐私化 ⑦ 注册/登录密码升级 PBKDF2（兼容旧用户）⑧ IP 哈希化 ⑨ 新增 scheduled 定时清理 + purge-privacy 端点 |
| `ai-ask-worker.js` | ① 顶部插入同一份安全工具模块 ② IP 哈希化、userId 脱敏兜底 ③ 用户统计记录去掉明文 IP |
| `admin.html` | ① 登录改为「密码换 Token」，sessionStorage 只存 Token ② 新增 getAuthHeaders()/adminFetch() 封装 ③ 全部 7 处 X-Admin-Password 调用改用 Token ④ IP 列表/注册审核展示脱敏 |
| `hash-password.mjs` | **新建**：本地生成 ADMIN_PASSWORD_HASH 的工具脚本 |
| `wrangler-stats.toml` | 加 `[triggers]` 定时清理配置（可选但推荐） |

---

## 4. 改动详情 — stats-auth-worker.js

### 4.1 新增：安全工具模块（整段复制）

在 `export default {` 之前插入以下整段代码（约 190 行，无任何外部依赖，全部使用 Workers 内置 Web Crypto）：

```js
// ============================================================
// 安全与隐私工具模块（v1）
// ============================================================

const PBKDF2_ITERATIONS = 100000;
const ADMIN_TOKEN_TTL_MS = 2 * 60 * 60 * 1000; // admin token 有效期 2 小时
const ADMIN_RL_WINDOW_MS = 15 * 60 * 1000;     // 失败统计窗口 15 分钟
const ADMIN_RL_MAX_FAILS = 5;                  // 窗口内最大失败次数
const ADMIN_RL_LOCK_MS = 30 * 60 * 1000;       // 触发后锁定 30 分钟

function jsonResp(obj, status = 200, extraHeaders = {}) {
  return new Response(JSON.stringify(obj), {
    status,
    headers: { 'Content-Type': 'application/json', 'Access-Control-Allow-Origin': '*', ...extraHeaders },
  });
}

// ---------- 常量时间比较（防时序攻击） ----------
function timingSafeEqual(a, b) {
  if (typeof a !== 'string' || typeof b !== 'string') return false;
  const len = Math.max(a.length, b.length);
  let diff = a.length === b.length ? 0 : 1;
  for (let i = 0; i < len; i++) {
    diff |= (a.charCodeAt(i) || 0) ^ (b.charCodeAt(i) || 0);
  }
  return diff === 0;
}

// ---------- 哈希基础 ----------
function bufToHex(buf) {
  return [...new Uint8Array(buf)].map(b => b.toString(16).padStart(2, '0')).join('');
}

async function sha256Hex(str) {
  const digest = await crypto.subtle.digest('SHA-256', new TextEncoder().encode(str));
  return bufToHex(digest);
}

async function hmacSha256Hex(keyStr, msg) {
  const enc = new TextEncoder();
  const key = await crypto.subtle.importKey('raw', enc.encode(keyStr), { name: 'HMAC', hash: 'SHA-256' }, false, ['sign']);
  const sig = await crypto.subtle.sign('HMAC', key, enc.encode(msg));
  return bufToHex(sig);
}

// ---------- PBKDF2 密码哈希 ----------
// 存储格式：pbkdf2$<iterations>$<salt_b64>$<hash_b64>
async function pbkdf2Hash(password, iterations = PBKDF2_ITERATIONS) {
  const enc = new TextEncoder();
  const salt = crypto.getRandomValues(new Uint8Array(16));
  const key = await crypto.subtle.importKey('raw', enc.encode(password), 'PBKDF2', false, ['deriveBits']);
  const bits = await crypto.subtle.deriveBits({ name: 'PBKDF2', hash: 'SHA-256', salt, iterations }, key, 256);
  const b64 = buf => btoa(String.fromCharCode(...new Uint8Array(buf)));
  return `pbkdf2$${iterations}$${b64(salt)}$${b64(bits)}`;
}

async function pbkdf2Verify(password, stored) {
  try {
    if (typeof stored !== 'string' || !stored.startsWith('pbkdf2$')) return false;
    const [, iterStr, saltB64, hashB64] = stored.split('$');
    const iterations = parseInt(iterStr, 10);
    if (!iterations || !saltB64 || !hashB64) return false;
    const enc = new TextEncoder();
    const salt = Uint8Array.from(atob(saltB64), c => c.charCodeAt(0));
    const expect = Uint8Array.from(atob(hashB64), c => c.charCodeAt(0));
    const key = await crypto.subtle.importKey('raw', enc.encode(password), 'PBKDF2', false, ['deriveBits']);
    const bits = await crypto.subtle.deriveBits({ name: 'PBKDF2', hash: 'SHA-256', salt, iterations }, key, expect.length * 8);
    const got = new Uint8Array(bits);
    let diff = got.length === expect.length ? 0 : 1;
    for (let i = 0; i < Math.max(got.length, expect.length); i++) {
      diff |= (got[i] || 0) ^ (expect[i] || 0);
    }
    return diff === 0;
  } catch (e) {
    return false;
  }
}

// ---------- IP 加盐哈希（脱敏，不可反推） ----------
// 只取 SHA-256 前 64 bit（16 个 hex 字符），足够用于去重/计数，无法还原原始 IP
async function hashIp(ip, salt) {
  if (!ip || ip === 'unknown') return '';
  const h = await sha256Hex(salt + '|' + ip);
  return h.slice(0, 16);
}

function getIpSalt(env) {
  // 优先用独立盐；未配置则从密码材料派生（保证部署即生效）
  return env.IP_HASH_SALT || 'ip-salt-v1|' + (env.ADMIN_PASSWORD_HASH || env.ADMIN_PASSWORD || 'static-salt');
}

async function getClientIpHash(request, env) {
  const ip = request.headers.get('CF-Connecting-IP') ||
             request.headers.get('X-Forwarded-For')?.split(',')[0] || '';
  return hashIp(ip, getIpSalt(env));
}

// ---------- 签名 Token（HMAC-SHA256，带过期时间，无状态） ----------
function getAdminTokenSecret(env) {
  if (env.ADMIN_TOKEN_SECRET) return env.ADMIN_TOKEN_SECRET;
  // 未显式配置时从密码材料派生：轮换密码即全局失效所有 token
  return 'admin-token-v1|' + (env.ADMIN_PASSWORD_HASH || env.ADMIN_PASSWORD || 'unset');
}

async function signPayloadToken(secret, payloadObj) {
  const payload = { iat: Date.now(), exp: Date.now() + ADMIN_TOKEN_TTL_MS, ...payloadObj };
  const body = btoa(JSON.stringify(payload)).replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
  const sig = await hmacSha256Hex(secret, body);
  return body + '.' + sig;
}

async function verifyPayloadToken(secret, token, expectType) {
  try {
    if (!token || typeof token !== 'string') return null;
    const dot = token.indexOf('.');
    if (dot < 0) return null;
    const body = token.slice(0, dot);
    const sig = token.slice(dot + 1);
    const expect = await hmacSha256Hex(secret, body);
    if (!timingSafeEqual(sig, expect)) return null;
    let json = body.replace(/-/g, '+').replace(/_/g, '/');
    while (json.length % 4) json += '=';
    const payload = JSON.parse(atob(json));
    if (!payload || typeof payload.exp !== 'number' || Date.now() > payload.exp) return null;
    if (expectType && payload.t !== expectType) return null;
    return payload;
  } catch (e) {
    return null;
  }
}

function issueAdminToken(env) {
  return signPayloadToken(getAdminTokenSecret(env), { t: 'admin' });
}

// ---------- 管理员密码验证 ----------
// 优先验证 ADMIN_PASSWORD_HASH（PBKDF2）；未配置哈希时兼容明文 ADMIN_PASSWORD。
// 注意：两者都未配置时一律拒绝（不再有默认密码），部署前必须先配置其一。
async function verifyAdminPassword(env, input) {
  if (!input || typeof input !== 'string') return false;
  if (env.ADMIN_PASSWORD_HASH) return pbkdf2Verify(input, env.ADMIN_PASSWORD_HASH);
  if (env.ADMIN_PASSWORD) return timingSafeEqual(input, env.ADMIN_PASSWORD);
  return false;
}

// ---------- 登录限速（内存层 + KV 层双保险） ----------
const _adminRlMem = new Map();

function _rlKey(ipHash) { return 'admin_rl_' + (ipHash || 'unknown'); }

async function adminRateCheck(kv, ipHash) {
  const now = Date.now();
  const mem = _adminRlMem.get(ipHash);
  if (mem && mem.lockUntil > now) {
    return { allowed: false, retryAfterSec: Math.ceil((mem.lockUntil - now) / 1000) };
  }
  if (kv) {
    const rec = await kv.get(_rlKey(ipHash), 'json').catch(() => null);
    if (rec && rec.lockUntil > now) {
      _adminRlMem.set(ipHash, rec);
      return { allowed: false, retryAfterSec: Math.ceil((rec.lockUntil - now) / 1000) };
    }
  }
  return { allowed: true, retryAfterSec: 0 };
}

async function adminRateFail(kv, ipHash) {
  const now = Date.now();
  let rec = _adminRlMem.get(ipHash);
  if (!rec || now - rec.windowStart > ADMIN_RL_WINDOW_MS) {
    rec = { fails: 0, windowStart: now, lockUntil: 0 };
  }
  rec.fails++;
  if (rec.fails >= ADMIN_RL_MAX_FAILS) {
    rec.lockUntil = now + ADMIN_RL_LOCK_MS;
    rec.fails = 0;
    rec.windowStart = now;
  }
  _adminRlMem.set(ipHash, rec);
  if (kv) {
    try { await kv.put(_rlKey(ipHash), JSON.stringify(rec), { expirationTtl: 1800 }); } catch (e) {}
  }
}

async function adminRateClear(kv, ipHash) {
  _adminRlMem.delete(ipHash);
  if (kv) { try { await kv.delete(_rlKey(ipHash)); } catch (e) {} }
}

// ---------- 统一 admin 认证入口 ----------
// 优先 X-Admin-Token（签名 token）；兼容过渡期的 X-Admin-Password（同样有限速保护）。
// 认证通过返回 null；失败返回可直接 return 的 Response。
async function verifyAdmin(request, env) {
  const kv = env.STATS_KV;
  const token = request.headers.get('X-Admin-Token') || '';
  const pwd = request.headers.get('X-Admin-Password') || '';

  if (token) {
    const payload = await verifyPayloadToken(getAdminTokenSecret(env), token, 'admin');
    if (payload) return null;
  }
  if (pwd) {
    const ipHash = (await getClientIpHash(request, env)) || 'unknown';
    const rl = await adminRateCheck(kv, ipHash);
    if (!rl.allowed) {
      return jsonResp({ success: false, message: `尝试次数过多，请 ${Math.ceil(rl.retryAfterSec / 60)} 分钟后再试` }, 429, { 'Retry-After': String(rl.retryAfterSec) });
    }
    if (await verifyAdminPassword(env, pwd)) {
      await adminRateClear(kv, ipHash);
      return null;
    }
    await adminRateFail(kv, ipHash);
    return jsonResp({ success: false, message: '管理员密码错误' }, 403);
  }
  return jsonResp({ success: false, message: '未认证' }, 401);
}

// ---------- 联系方式打码（后台展示层使用） ----------
function maskContact(s) {
  if (!s || typeof s !== 'string') return '';
  return s
    .replace(/(1[3-9]\d)\d{4}(\d{4})/g, '$1****$2')
    .replace(/([A-Za-z0-9._+-]+)@([A-Za-z0-9-]+\.[A-Za-z0-9.-]+)/g, (m, u, d) => u.slice(0, 2) + '***@' + d);
}

// ---------- 过期数据自动清理（由 Cron 触发） ----------
async function purgeExpiredData(env) {
  const kv = env.STATS_KV;
  if (!kv) return { deleted: 0 };
  const now = Date.now();
  const DAY = 86400000;
  const dailyDays = parseInt(env.RETENTION_DAILY_DAYS || '90', 10);
  const userDays = parseInt(env.RETENTION_USER_DAYS || '365', 10);
  const deviceDays = parseInt(env.RETENTION_DEVICE_DAYS || '365', 10);
  const ipDays = parseInt(env.RETENTION_IP_DAYS || '180', 10);
  let deleted = 0;

  // 1) AI 问答每日统计：超期删除
  let cursor = '';
  do {
    const r = await kv.list({ prefix: 'ai_ask_daily_', cursor, limit: 1000 });
    for (const k of r.keys) {
      const t = Date.parse(k.name.replace('ai_ask_daily_', ''));
      if (t && now - t > dailyDays * DAY) { await kv.delete(k.name); deleted++; }
    }
    cursor = r.list_complete ? '' : r.cursor;
  } while (cursor);

  // 2) AI 问答用户记录：长期不活跃删除
  cursor = '';
  do {
    const r = await kv.list({ prefix: 'ai_ask_user_', cursor, limit: 1000 });
    for (const k of r.keys) {
      const rec = await kv.get(k.name, 'json').catch(() => null);
      if (rec && rec.lastCallTime && now - rec.lastCallTime > userDays * DAY) {
        await kv.delete(k.name); deleted++;
      }
    }
    cursor = r.list_complete ? '' : r.cursor;
  } while (cursor);

  // 3) 设备记录：长期不活跃删除，并同步 unique_devices 列表
  cursor = '';
  let devicePurged = false;
  do {
    const r = await kv.list({ prefix: 'device_', cursor, limit: 1000 });
    for (const k of r.keys) {
      const rec = await kv.get(k.name, 'json').catch(() => null);
      if (rec && rec.lastSeen && now - Date.parse(rec.lastSeen) > deviceDays * DAY) {
        await kv.delete(k.name); deleted++; devicePurged = true;
      }
    }
    cursor = r.list_complete ? '' : r.cursor;
  } while (cursor);
  if (devicePurged) {
    const alive = new Set();
    cursor = '';
    do {
      const r = await kv.list({ prefix: 'device_', cursor, limit: 1000 });
      for (const k of r.keys) alive.add(k.name.slice('device_'.length));
      cursor = r.list_complete ? '' : r.cursor;
    } while (cursor);
    await kv.put('unique_devices', JSON.stringify([...alive]));
  }

  // 4) IP（已哈希）记录：长期不活跃删除，并同步 unique_ips 列表
  cursor = '';
  let ipPurged = false;
  do {
    const r = await kv.list({ prefix: 'ip_', cursor, limit: 1000 });
    for (const k of r.keys) {
      const rec = await kv.get(k.name, 'json').catch(() => null);
      if (rec && rec.lastSeen && now - Date.parse(rec.lastSeen) > ipDays * DAY) {
        await kv.delete(k.name); deleted++; ipPurged = true;
      }
    }
    cursor = r.list_complete ? '' : r.cursor;
  } while (cursor);
  if (ipPurged) {
    const alive = new Set();
    cursor = '';
    do {
      const r = await kv.list({ prefix: 'ip_', cursor, limit: 1000 });
      for (const k of r.keys) alive.add(k.name.slice('ip_'.length));
      cursor = r.list_complete ? '' : r.cursor;
    } while (cursor);
    await kv.put('unique_ips', JSON.stringify([...alive]));
  }

  return { deleted };
}

// ============================================================
// 安全与隐私工具模块结束
// ============================================================
```

### 4.2 修改：CORS 允许头

**旧：**
```js
      'Access-Control-Allow-Headers': 'Content-Type, X-Device-ID, X-Admin-Password',
```
**新：**
```js
      'Access-Control-Allow-Headers': 'Content-Type, X-Device-ID, X-Admin-Password, X-Admin-Token',
```

### 4.3 新增路由：管理员登录（换 Token）

在 `// 路由：管理员创建邀请码` 之前插入：

```js
      // 路由：管理员登录（密码换签名 token，明文密码只在此接口出现一次）
      if (path === '/api/admin/login' && request.method === 'POST') {
        const ipHash = (await getClientIpHash(request, env)) || 'unknown';
        const rl = await adminRateCheck(env.STATS_KV, ipHash);
        if (!rl.allowed) {
          return jsonResp({ success: false, message: `尝试次数过多，请 ${Math.ceil(rl.retryAfterSec / 60)} 分钟后再试` }, 429, { 'Retry-After': String(rl.retryAfterSec) });
        }
        const body = await request.json().catch(() => ({}));
        if (await verifyAdminPassword(env, body.password || '')) {
          await adminRateClear(env.STATS_KV, ipHash);
          const token = await issueAdminToken(env);
          return new Response(JSON.stringify({
            success: true,
            token: token,
            expiresInSec: Math.floor(ADMIN_TOKEN_TTL_MS / 1000),
          }), {
            headers: { ...corsHeaders, 'Content-Type': 'application/json' },
          });
        }
        await adminRateFail(env.STATS_KV, ipHash);
        return new Response(JSON.stringify({ success: false, message: '管理员密码错误' }), {
          status: 403,
          headers: { ...corsHeaders, 'Content-Type': 'application/json' },
        });
      }
```

### 4.4 替换：9 处 admin 鉴权统一改用 verifyAdmin

以下 9 个路由的「密码校验段」全部替换为统一的三行写法。每处给出现状定位（旧代码以实际文件为准，搜索特征串即可）：

**统一新写法**（插在每个路由的 `if (path === ...)` 判断内部、业务逻辑之前）：

```js
        const adminAuthError = await verifyAdmin(request, env);
        if (adminAuthError) return adminAuthError;
```

需要替换的 9 处及旧代码特征：

| # | 路由 | 删除的旧代码特征（搜索用） |
|---|------|---------------------------|
| 1 | `/api/admin/invite/create` | `adminPassword !== (env.ADMIN_PASSWORD \|\| 'admin610')` |
| 2 | `/api/admin/invite/list` | 同上 |
| 3 | `/api/admin/device/rename` | `adminPass !== (env.ADMIN_PASSWORD \|\| 'admin610')`（**注意**：此行同时含 `\|\| url.searchParams.get('admin')`，一并删除） |
| 4 | `/api/admin/stats` | `adminPass !== env.ADMIN_PASSWORD`（此行也含 URL 参数 fallback，一并删除） |
| 5 | `/api/admin/pending` | 同上 |
| 6 | `/api/admin/review` | `adminPass !== env.ADMIN_PASSWORD` |
| 7 | `/api/admin/clear-stats` | `adminPass !== (env.ADMIN_PASSWORD \|\| 'admin610')`（含 URL 参数 fallback） |
| 8 | `/api/admin/set-password-status` | `adminPass !== env.ADMIN_PASSWORD` |
| 9 | `/api/admin/create-temp-link` | `correctAdminPassword && adminPassword !== correctAdminPassword`（这段还带 `if (correctAdminPassword && ...)` 外层条件，整体换成统一三行） |

**示例（#1 完整对照）：**

旧：
```js
        const adminPassword = request.headers.get('X-Admin-Password');
        if (adminPassword !== (env.ADMIN_PASSWORD || 'admin610')) {
          return new Response(JSON.stringify({ success: false, message: '管理员密码错误' }), {
            status: 403,
            headers: { ...corsHeaders, 'Content-Type': 'application/json' },
          });
        }
```
新：
```js
        const adminAuthError = await verifyAdmin(request, env);
        if (adminAuthError) return adminAuthError;
```

> ⚠️ 关键要求：**替换后文件中不得再出现字符串 `admin610`，不得再出现 `url.searchParams.get('admin')`**。全部替换完成后全局搜索确认（共应 0 命中）。

### 4.5 修改：/api/track 记录设备（IP 哈希化 + 删除经纬度）

**旧：**
```js
        if (deviceId) {
          const geo = {
            country: country,
            region: request.cf?.region || '',
            city: request.cf?.city || '',
            postalCode: request.cf?.postalCode || '',
            latitude: request.cf?.latitude || '',
            longitude: request.cf?.longitude || '',
            timezone: request.cf?.timezone || '',
          };
          await recordDevice(env.STATS_KV, deviceId, clientIP, geo);
        }
```
**新：**
```js
        if (deviceId) {
          const geo = {
            country: country,
            city: request.cf?.city || '',
            timezone: request.cf?.timezone || '',
          };
          const ipHash = await getClientIpHash(request, env);
          await recordDevice(env.STATS_KV, deviceId, ipHash, geo);
        }
```
（只保留国家/城市/时区，删除省份、邮编、经纬度——精确定位信息不再落库。）

### 4.6 修改：recordDevice 函数（整函数替换）

**旧：**（从 `async function recordDevice(kv, deviceId, ip, geo) {` 到该函数结束，约 L1257-1328）
**新：**
```js
/**
 * 记录设备（累计，不重置）
 * 隐私原则：不存原始 IP；IP 只存加盐哈希；不存经纬度/邮编
 */
async function recordDevice(kv, deviceId, ipHash, geo) {
  if (!kv || !deviceId) return;
  const country = geo?.country || 'UNKNOWN';

  // 记录设备（去重）
  const key = 'unique_devices';
  const existing = await kv.get(key, 'json');
  const deviceSet = new Set(existing || []);

  if (!deviceSet.has(deviceId)) {
    deviceSet.add(deviceId);
    await kv.put(key, JSON.stringify([...deviceSet]));

    // 记录设备详情（不含 IP，地理位置只到城市级）
    await kv.put('device_' + deviceId, JSON.stringify({
      deviceId,
      country: country,
      city: geo?.city || '',
      timezone: geo?.timezone || '',
      firstSeen: new Date().toISOString(),
      lastSeen: new Date().toISOString(),
      visitCount: 1,
      visitorType: 'unknown', // admin / bot / real
    }));
  } else {
    // 已存在的设备：更新最近访问时间和访问次数
    const deviceDetail = await kv.get('device_' + deviceId, 'json');
    if (deviceDetail) {
      deviceDetail.lastSeen = new Date().toISOString();
      deviceDetail.visitCount = (deviceDetail.visitCount || 0) + 1;
      await kv.put('device_' + deviceId, JSON.stringify(deviceDetail));
    }
  }

  // 记录 IP（去重）——键与值都使用加盐哈希，不可反推
  if (ipHash) {
    const hashKey = 'ip_' + ipHash;
    const existingIps = await kv.get('unique_ips', 'json');
    const ipSet = new Set(existingIps || []);

    if (!ipSet.has(ipHash)) {
      ipSet.add(ipHash);
      await kv.put('unique_ips', JSON.stringify([...ipSet]));

      await kv.put(hashKey, JSON.stringify({
        ipHash: ipHash,
        country: country,
        city: geo?.city || '',
        timezone: geo?.timezone || '',
        firstSeen: new Date().toISOString(),
        lastSeen: new Date().toISOString(),
        visitCount: 1,
      }));
    } else {
      const ipDetail = await kv.get(hashKey, 'json');
      if (ipDetail) {
        ipDetail.lastSeen = new Date().toISOString();
        ipDetail.visitCount = (ipDetail.visitCount || 0) + 1;
        await kv.put(hashKey, JSON.stringify(ipDetail));
      }
    }
  }
}
```

### 4.7 修改：/api/register（密码 PBKDF2 + IP 脱敏）

**旧（两处）：**
```js
        // 简单密码哈希（实际生产应使用bcrypt，这里用简单哈希）
        const passwordHash = simpleHash(password);
```
**新：**
```js
        // PBKDF2 加盐哈希（10 万次迭代，抗暴力破解）
        const passwordHash = await pbkdf2Hash(password);
```

**旧：**
```js
          deviceId: deviceId,
          ip: clientIP,
          country: country,
```
**新：**
```js
          deviceId: deviceId,
          ipHash: await getClientIpHash(request, env),
          country: country,
```

### 4.8 修改：/api/login（兼容旧用户 + 自动升级哈希）

**旧：**
```js
        // 验证密码
        const passwordHash = simpleHash(password);
        if (user.passwordHash !== passwordHash) {
          return new Response(JSON.stringify({ success: false, message: '密码错误' }), {
            status: 401,
            headers: { ...corsHeaders, 'Content-Type': 'application/json' },
          });
        }
```
**新：**
```js
        // 验证密码：优先 PBKDF2；旧用户（simpleHash 格式）兼容验证并自动升级哈希
        let passwordOk = await pbkdf2Verify(password, user.passwordHash);
        if (!passwordOk && typeof user.passwordHash === 'string' && user.passwordHash.startsWith('h_')) {
          passwordOk = timingSafeEqual(simpleHash(password), user.passwordHash);
          if (passwordOk) {
            user.passwordHash = await pbkdf2Hash(password); // 自动升级到 PBKDF2
          }
        }
        if (!passwordOk) {
          return new Response(JSON.stringify({ success: false, message: '密码错误' }), {
            status: 401,
            headers: { ...corsHeaders, 'Content-Type': 'application/json' },
          });
        }
```

**旧（登录 token）：**
```js
        // 生成登录token（简单实现，实际应使用JWT）
        const token = simpleHash(username + Date.now() + Math.random());
```
**新：**
```js
        // 生成登录 token（HMAC-SHA256 签名，7 天有效，不可伪造）
        const token = await signPayloadToken('user-token-v1|' + getAdminTokenSecret(env), { t: 'user', u: username });
```
> 注：`signPayloadToken` 默认 exp 是 2 小时（ADMIN_TOKEN_TTL_MS），用户 token 有效期短一些也可接受；如需 7 天，可在 payloadObj 里加 `exp: Date.now() + 7*86400000` 覆盖。保持 2 小时不影响功能（前端会重新登录）。

### 4.9 新增路由：一次性隐私清理（部署后手动调用一次）

在 `// 路由：管理员清理历史统计数据` 之前插入：

```js
      // 路由：一次性隐私清理（删除历史明文 IP 数据）
      // 部署本版本后调用一次即可：POST /api/admin/purge-privacy（带 X-Admin-Token 头）
      // 注意：执行后 IP 统计数将从零重新累计（旧明文 IP 记录被删除，无法补救成哈希）
      if (path === '/api/admin/purge-privacy' && request.method === 'POST') {
        const adminAuthError = await verifyAdmin(request, env);
        if (adminAuthError) return adminAuthError;

        try {
          let deleted = 0;
          let cursor = '';

          // 1) 删除所有历史明文 ip_* 记录，并重置 unique_ips 列表
          do {
            const r = await env.STATS_KV.list({ prefix: 'ip_', cursor, limit: 1000 });
            for (const k of r.keys) {
              await env.STATS_KV.delete(k.name);
              deleted++;
            }
            cursor = r.list_complete ? '' : r.cursor;
          } while (cursor);
          await env.STATS_KV.put('unique_ips', JSON.stringify([]));

          // 2) 清除 device_* 记录中的历史明文 IP 字段（firstIp）
          cursor = '';
          let cleaned = 0;
          do {
            const r = await env.STATS_KV.list({ prefix: 'device_', cursor, limit: 1000 });
            for (const k of r.keys) {
              const rec = await env.STATS_KV.get(k.name, 'json').catch(() => null);
              if (rec && rec.firstIp !== undefined) {
                delete rec.firstIp;
                await env.STATS_KV.put(k.name, JSON.stringify(rec));
                cleaned++;
              }
            }
            cursor = r.list_complete ? '' : r.cursor;
          } while (cursor);

          // 3) 清除注册用户记录中的历史明文 IP 字段
          cursor = '';
          do {
            const r = await env.STATS_KV.list({ prefix: 'user_', cursor, limit: 1000 });
            for (const k of r.keys) {
              const rec = await env.STATS_KV.get(k.name, 'json').catch(() => null);
              if (rec && rec.ip !== undefined) {
                delete rec.ip;
                await env.STATS_KV.put(k.name, JSON.stringify(rec));
              }
            }
            cursor = r.list_complete ? '' : r.cursor;
          } while (cursor);

          return new Response(JSON.stringify({
            success: true,
            message: `隐私清理完成：删除明文 IP 记录 ${deleted} 条，清理设备记录 ${cleaned} 条`,
          }), {
            headers: { ...corsHeaders, 'Content-Type': 'application/json' },
          });
        } catch (e) {
          return new Response(JSON.stringify({ success: false, error: e.message }), {
            status: 500,
            headers: { ...corsHeaders, 'Content-Type': 'application/json' },
          });
        }
      }
```

### 4.10 新增：scheduled 定时清理（加在 `async fetch(...)` 同级）

`export default { async fetch(request, env, ctx) {...}, }` 中，在 fetch 方法后面加一个 scheduled 方法（注意 fetch 方法结束的 `},` 后加逗号）：

```js
  // 定时任务：每日 03:00 UTC 自动清理过期统计数据（需在 wrangler.toml 配置 [triggers]）
  async scheduled(controller, env, ctx) {
    ctx.waitUntil(purgeExpiredData(env).then(r => console.log('定时清理完成:', r.deleted)).catch(e => console.error('定时清理失败:', e)));
  },
```

---

## 5. 改动详情 — ai-ask-worker.js

### 5.1 插入安全工具模块

与 4.1 节完全相同的整段代码，插入到 `export default {` 之前。
（只需其中实际用到的 hashIp/getIpSalt/sha256Hex 部分，但为了两个文件保持一致、便于日后维护，**整段复制**即可。）

### 5.2 修改：客户端标识脱敏

**旧：**
```js
    const deviceId = request.headers.get('X-Device-ID') || '';
    const userId = deviceId || clientIP; // 优先用设备ID，没有则用IP
```
**新：**
```js
    const deviceId = request.headers.get('X-Device-ID') || '';
    // 隐私化：无设备 ID 时用加盐哈希后的 IP 作为兜底标识，原始 IP 不参与任何存储
    const ipHash = await hashIp(clientIP, getIpSalt(env));
    const userId = deviceId || ('anon_' + (ipHash || 'unknown'));
```

### 5.3 修改：三处 recordAiAsk 调用中的 IP 字段

三处（成功、失败、异常各一处）把 `ip: clientIP,` 全部改为 `ipHash: ipHash,`：

**旧：**
```js
          ip: clientIP,
```
**新：**
```js
          ipHash: ipHash,
```
（共 3 处，全部替换；`question.slice(0, 200)` 的截断保留不动。）

### 5.4 修改：用户统计记录去掉明文 IP

**旧：**
```js
      userStats = {
        userId: record.userId,
        deviceId: record.deviceId,
        ip: record.ip,
        totalCalls: 0,
        successCalls: 0,
        failedCalls: 0,
        totalTokens: 0,
        firstCallTime: record.timestamp,
        lastCallTime: record.timestamp,
        recentQuestions: [],
      };
```
**新：**
```js
      userStats = {
        userId: record.userId,
        deviceId: record.deviceId || '',
        totalCalls: 0,
        successCalls: 0,
        failedCalls: 0,
        totalTokens: 0,
        firstCallTime: record.timestamp,
        lastCallTime: record.timestamp,
        recentQuestions: [],
      };
```
（`ip: record.ip,` 一行删除；deviceId 是随机生成的设备标识，非个人身份信息，保留用于区分设备；原始 IP 字段彻底移除。注意：历史已存在的 `ai_ask_user_*` 记录中可能残留明文 ip 字段，由一次性清理或保留期自动过期，无需专门迁移。）

> 说明：`ai_ask_recent_logs` 与用户 `recentQuestions` 中保留**问题原文前 200 字符**——这属于用户要求的「用户习惯」类运营数据，已做截断，且记录不与原始 IP 关联（userId 为随机设备 ID 或哈希）。如日后想更激进，可把 `record.question` 改为 `record.question.slice(0, 50)` 或整体不存，属可选增强，本次不做。

---

## 6. 改动详情 — admin.html

### 6.1 整块替换：登录逻辑（约 L349-405）

**旧（从 `let adminPassword = '';` 到 logout 函数结束）：**
```js
let adminPassword = '';

// 检查是否已登录
function checkLogin() {
  const saved = sessionStorage.getItem('adminPassword');
  if (saved) {
    adminPassword = saved;
    showAdmin();
    loadAll();
  } else {
    showLogin();
  }
}
```
（……以及 login()、logout() 两个函数，见原文件 L373-405）

**新（完整替换为以下代码块）：**
```js
let adminToken = '';

// 统一认证头：所有管理接口都用签名 Token，明文密码只在登录接口出现一次
function getAuthHeaders() {
  return { 'X-Admin-Token': adminToken };
}

// 统一请求封装：自动带 Token，401 时自动登出
async function adminFetch(url, options = {}) {
  const res = await fetch(url, {
    ...options,
    headers: { ...getAuthHeaders(), ...(options.headers || {}) },
  });
  if (res.status === 401) {
    logout();
    throw new Error('登录已过期，请重新登录');
  }
  return res;
}

// 检查是否已登录
function checkLogin() {
  const saved = sessionStorage.getItem('adminToken');
  if (saved) {
    adminToken = saved;
    showAdmin();
    loadAll();
  } else {
    showLogin();
  }
}
```

**login() 函数替换：**

旧：
```js
function login() {
  const pwd = document.getElementById('adminPassword').value;
  if (!pwd) {
    document.getElementById('loginError').textContent = '请输入密码';
    return;
  }
  
  // 验证密码（调用待审核 API 测试）
  fetch(API_BASE + '/api/admin/pending', {
    headers: { 'X-Admin-Password': pwd }
  })
  .then(r => r.json())
  .then(data => {
    if (data.success) {
      adminPassword = pwd;
      sessionStorage.setItem('adminPassword', pwd);
      showAdmin();
      loadAll();
    } else {
      document.getElementById('loginError').textContent = data.message || '密码错误';
    }
  })
  .catch(err => {
    document.getElementById('loginError').textContent = '网络错误：' + err.message;
  });
}
```

新：
```js
function login() {
  const pwd = document.getElementById('adminPassword').value;
  if (!pwd) {
    document.getElementById('loginError').textContent = '请输入密码';
    return;
  }

  // 新流程：密码换签名 Token（2 小时有效），密码错误 5 次将锁定 30 分钟
  fetch(API_BASE + '/api/admin/login', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ password: pwd })
  })
  .then(r => r.json())
  .then(data => {
    if (data.success && data.token) {
      adminToken = data.token;
      sessionStorage.setItem('adminToken', data.token);
      document.getElementById('adminPassword').value = '';
      showAdmin();
      loadAll();
    } else {
      document.getElementById('loginError').textContent = data.message || '密码错误';
    }
  })
  .catch(err => {
    document.getElementById('loginError').textContent = '网络错误：' + err.message;
  });
}
```

**logout() 函数替换：**

旧：
```js
function logout() {
  sessionStorage.removeItem('adminPassword');
  adminPassword = '';
  document.getElementById('adminPassword').value = '';
  showLogin();
}
```
新：
```js
function logout() {
  sessionStorage.removeItem('adminToken');
  adminToken = '';
  document.getElementById('adminPassword').value = '';
  showLogin();
}
```

### 6.2 全局替换：7 处 X-Admin-Password 请求头

在 admin.html 全文搜索 `X-Admin-Password`（共 7 处），按下面规则替换。原则：`adminPassword` 变量已不存在，统一改用 `adminFetch` 或 `getAuthHeaders()`。

**模式 A — 简单 GET（4 处：loadStats/loadPending/loadInvites/loadAiAskStats 等），形如：**

旧：
```js
  fetch(API_BASE + '/api/admin/stats', {
    headers: { 'X-Admin-Password': adminPassword }
  })
```
新：
```js
  adminFetch(API_BASE + '/api/admin/stats')
```
（即：`fetch(..., { headers: { 'X-Admin-Password': adminPassword } })` → `adminFetch(...)`，去掉整个 options 对象。）

**模式 B — 带 body 的 POST（约 3 处：createInvite/review/renameDevice 等），形如：**

旧：
```js
  fetch(API_BASE + '/api/admin/invite/create', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'X-Admin-Password': adminPassword
    },
    body: JSON.stringify({ code: '', maxUses: 0, expiresAt: null, note: '' })
  })
```
新：
```js
  adminFetch(API_BASE + '/api/admin/invite/create', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ code: '', maxUses: 0, expiresAt: null, note: '' })
  })
```
（即：`fetch(` 换成 `adminFetch(`，headers 里的 `'X-Admin-Password': adminPassword` 一行删除，body 等其余参数原样保留。）

> 具体行号参考（改前）：L416（loadStats）、L672（保存邀请码）、L827（loadPending）、L875（review）、L1089（loadAiAskStats）、L1145（renameDevice）。以搜索结果为准，**改完后全文搜索 `X-Admin-Password` 和 `adminPassword` 应为 0 命中**（注意 L159 的 `id="adminPassword"` 输入框 id 保留不动，它只是输入框 DOM id；若搜索 `adminPassword` 命中该 DOM id 属正常，只需确保没有 `sessionStorage.getItem('adminPassword')` 和 `'X-Admin-Password'` 残留）。

### 6.3 修改：IP 列表展示脱敏（renderIpList，约 L690-711）

**旧：**
```js
  let html = '<table><tr><th>IP 地址</th><th>国家/地区</th><th>省份</th><th>城市</th><th>首次访问时间</th><th>最近访问时间</th><th>访问次数</th></tr>';
  ips.reverse().forEach(ip => {
    html += `<tr>
      <td style="font-family:monospace;">${ip.ip}</td>
      <td>${ip.country}</td>
      <td>${ip.region || '-'}</td>
      <td>${ip.city || '-'}</td>
      <td>${formatTime(ip.firstSeen)}</td>
      <td>${formatTime(ip.lastSeen)}</td>
      <td>${ip.visitCount || '-'}</td>
    </tr>`;
  });
```
**新：**
```js
  let html = '<table><tr><th>IP 标识（已脱敏）</th><th>国家/地区</th><th>城市</th><th>首次访问时间</th><th>最近访问时间</th><th>访问次数</th></tr>';
  ips.reverse().forEach(ip => {
    html += `<tr>
      <td style="font-family:monospace;" title="IP 已加盐哈希脱敏，无法还原">${(ip.ipHash || '').slice(0, 8) || '-'}</td>
      <td>${ip.country}</td>
      <td>${ip.city || '-'}</td>
      <td>${formatTime(ip.firstSeen)}</td>
      <td>${formatTime(ip.lastSeen)}</td>
      <td>${ip.visitCount || '-'}</td>
    </tr>`;
  });
```

### 6.4 修改：注册审核展示脱敏（约 L842-848）

**旧（loadPending 中渲染注册审核表的片段，反引号内模板字符串）：**
```js
    let html = '<table><tr><th>昵称</th><th>申请理由</th><th>联系方式</th><th>IP</th><th>申请时间</th><th>操作</th></tr>';
    regs.forEach(r => {
      html += `<tr id="reg-${r.id}">
        <td><strong>${esc(r.nickname)}</strong></td>
        <td>${esc(r.reason)}</td>
        <td>${esc(r.contact || '-')}</td>
        <td style="font-family:monospace;font-size:0.8rem;">${r.ip}</td>`;
      /* ……此片段之后还有申请时间/操作列等模板内容，略…… */
    });
```
**新：**
```js
    let html = '<table><tr><th>昵称</th><th>申请理由</th><th>联系方式</th><th>IP（脱敏）</th><th>申请时间</th><th>操作</th></tr>';
    regs.forEach(r => {
      html += `<tr id="reg-${r.id}">
        <td><strong>${esc(r.nickname)}</strong></td>
        <td>${esc(r.reason)}</td>
        <td>${esc(maskContact(r.contact || '-'))}</td>
        <td style="font-family:monospace;font-size:0.8rem;" title="IP 已脱敏">${(r.ipHash || '').slice(0, 8) || '-'}</td>`;
      /* ……此片段之后还有申请时间/操作列等模板内容，保持原样…… */
    });
```
> 实际修改时只改「联系方式」和「IP」两个 `<td>` 的内容即可，其余行不动。

同时在 admin.html 的 `<script>` 内（getAuthHeaders 附近）加一个打码函数：

```js
// 联系方式打码：手机号/邮箱中间部分隐藏（后台展示层使用，原文仍存于服务端）
function maskContact(s) {
  if (!s || typeof s !== 'string') return '';
  return s
    .replace(/(1[3-9]\d)\d{4}(\d{4})/g, '$1****$2')
    .replace(/([A-Za-z0-9._+-]+)@([A-Za-z0-9-]+\.[A-Za-z0-9.-]+)/g, (m, u, d) => u.slice(0, 2) + '***@' + d);
}
```

---

## 7. 新文件与配置

### 7.1 新建 `hash-password.mjs`（项目根目录）

```js
#!/usr/bin/env node
// 生成管理员密码的 PBKDF2 哈希（Node 18+ 自带 crypto，无依赖）
// 用法：node hash-password.mjs 你的新密码
// 输出形如：pbkdf2$100000$xxxx$yyyy
// 把输出整行配置为 Cloudflare Worker 环境变量 ADMIN_PASSWORD_HASH
import { pbkdf2, randomBytes } from 'node:crypto';

const password = process.argv[2];
if (!password || password.length < 8) {
  console.error('用法：node hash-password.mjs <密码>（建议至少 12 位，混合大小写数字符号）');
  process.exit(1);
}
const iterations = 100000;
const salt = randomBytes(16);
pbkdf2(password, salt, iterations, 32, 'sha256', (err, hash) => {
  if (err) { console.error('生成失败:', err); process.exit(1); }
  console.log(`pbkdf2$${iterations}$${salt.toString('base64')}$${hash.toString('base64')}`);
});
```

### 7.2 环境变量配置（部署 Worker 前**必须先做**）

在项目根目录执行（需要已安装 wrangler，或用 `npx wrangler`）：

```bash
# 1. 本地生成新后台密码的哈希（密码自己想一个强密码，不要用旧密码 admin610）
node hash-password.mjs '你的新强密码'
# 复制输出的整行

# 2. 配置到 Worker（粘贴刚才的输出，回车）
npx wrangler secret put ADMIN_PASSWORD_HASH --config wrangler-stats.toml

# 3.（可选但推荐）独立 Token 签名密钥：随机长字符串
#    不配置也安全（会自动从密码哈希派生），配置后轮换更灵活
npx wrangler secret put ADMIN_TOKEN_SECRET --config wrangler-stats.toml

# 4.（可选）独立 IP 哈希盐
npx wrangler secret put IP_HASH_SALT --config wrangler-stats.toml
```

> ⚠️ **顺序铁律**：必须先配好 `ADMIN_PASSWORD_HASH`（或至少保留已配置的 `ADMIN_PASSWORD`），再部署新 Worker。新代码里默认密码已删除，两者都未配置时后台将无法登录（这是刻意的 fail-closed 设计）。

> ai-ask-worker 不需要任何新环境变量（它不用 admin 密码），安全工具模块里未用到的函数不影响运行。

### 7.3 wrangler-stats.toml 增加定时清理（推荐）

在文件末尾追加：

```toml
# 每日 03:00 UTC（北京时间 11:00）自动清理过期统计数据
[triggers]
crons = ["0 3 * * *"]
```

如通过 Cloudflare Dashboard 粘贴代码部署（非 wrangler CLI），则需在 Dashboard → Workers → longchen-stats-auth → Settings → Triggers → Cron Triggers 手动添加 `0 3 * * *`。

保留期可在环境变量覆盖（默认：每日统计 90 天、用户/设备记录 365 天、IP 记录 180 天）：`RETENTION_DAILY_DAYS` / `RETENTION_USER_DAYS` / `RETENTION_DEVICE_DAYS` / `RETENTION_IP_DAYS`。

### 7.4 部署顺序

```bash
# 1.（先做）按 7.2 配置 ADMIN_PASSWORD_HASH 等 secrets
# 2. 部署 stats Worker
npx wrangler deploy --config wrangler-stats.toml
# 3. 部署 ai-ask-worker（若用 Dashboard 粘贴方式，把改好的文件全文粘贴保存即可）
# 4. 执行一次性隐私清理（见 7.5）
# 5. 确认后台登录正常后：跑 python build_site.py 重新构建（admin.html 会复制到 dist/admin/index.html），发布到 Cloudflare Pages
```

### 7.5 一次性清理历史明文 IP（部署新 Worker 后执行一次）

浏览器登录新后台后，在控制台（F12）执行：

```js
fetch('https://api.longchen-nyingtik.wiki/api/admin/purge-privacy', {
  method: 'POST',
  headers: { 'X-Admin-Token': sessionStorage.getItem('adminToken') }
}).then(r => r.json()).then(console.log);
```

预期返回 `{"success":true,"message":"隐私清理完成：删除明文 IP 记录 N 条，清理设备记录 M 条"}`。
执行后「累计 IP 数」从零重新累计，属正常现象（旧明文数据已按隐私原则删除）。

---

## 8. 自测清单

**语法与静态检查：**
- [ ] `node --check stats-auth-worker.js` 通过
- [ ] `node --check ai-ask-worker.js` 通过
- [ ] 全局搜索确认：`admin610`（0 命中）、`searchParams.get('admin')`（0 命中）、stats-auth-worker 中 `firstIp`（0 命中）、`latitude`/`longitude`/`postalCode`（0 命中）
- [ ] admin.html 中 `X-Admin-Password`（0 命中）、`sessionStorage.getItem('adminPassword')`（0 命中）

**功能测试（部署后）：**
- [ ] `node hash-password.mjs 'test1234'` 输出 `pbkdf2$...` 格式
- [ ] 打开后台登录页，旧密码登录失败，新密码登录成功
- [ ] 登录后 sessionStorage 中只有 `adminToken`（无明文密码）
- [ ] 故意输错密码 5 次 → 第 6 次提示「尝试次数过多」，等 30 分钟或换 IP 才能再试
- [ ] 后台各面板（设备/IP/文章/音频/点击/AI 问答/邀请码）正常显示，IP 列表显示 8 位脱敏标识
- [ ] 网站前台正常打开，`/api/track` 正常返回，KV 中新产生的 `ip_` 键的键名是 16 位十六进制哈希（而非 IP 原文）
- [ ] AI 问答正常回答，后台 AI 统计正常
- [ ] Token 2 小时后过期：新开标签操作会自动弹回登录页（不报错）
- [ ] 清理任务：手动触发 `purge-privacy` 返回成功；Cron 可在 Dashboard 的 Trigger 最近执行记录里确认

---

## 9. 隐私 5 原则对照表（验收）

| # | 用户要求 | 实现方式 | 落点 |
|---|----------|----------|------|
| 1 | 不暴露用户信息，不涵盖个人隐私 | 删除经纬度/邮编/省份；设备记录不再存 IP；注册用户 ip 字段改 ipHash | 4.5 / 4.6 / 4.7 |
| 2 | 仅收集设备、IP（脱敏）、用户习惯 | IP 只以加盐 SHA-256 截断哈希存储，仅用于计数去重；设备信息保留类型/系统/屏幕等非个人字段 | 4.6 / 5.2 |
| 3 | 数据不与具体人名关联 | 全库无姓名字段；注册昵称/联系方式只存于用户自己账户，与统计数据完全分离；AI 问答记录只用随机设备 ID 或哈希标识 | 5.4 |
| 4 | 后台不展示个人信息 | 后台 IP 列表显示 8 位哈希短码；联系方式打码显示（1****5678 / ab***@qq.com） | 6.3 / 6.4 |
| 5 | 数据被黑客获取也无用 | ① IP 哈希加盐不可反推 ② 密码 PBKDF2 加盐 10 万次迭代 ③ Token HMAC 签名不可伪造 ④ 无明文密码/无明文 IP/无精确位置可泄露 | 4.1 / 4.7 / 7.2 |

---

## 10. 回滚方案

如果部署后出问题：Cloudflare Dashboard → Workers → 对应 Worker → Deployments → 选上一个版本 Rollback 即可，几分钟内恢复。admin.html 回滚则 git 还原后重新 build_site.py + 发 Pages。
