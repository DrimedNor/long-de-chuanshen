# 任务一：用户系统前端 — 代码改动交付

> **交付方式说明**：按任务书要求，未直接修改任何文件。以下全部改动**仅涉及 `build_site.py` 一个文件**，共 5 处，按编号顺序应用即可。所有行号为当前文件的大约行号，应用时以「旧代码」锚文本为准。

---

## 〇、改动总览

| 编号 | 位置 | 内容 | 大约行号 |
|------|------|------|----------|
| 改动1 | `<style>` 末尾 | 新增用户系统 CSS（登录/注册/用户中心） | ~1199（`</style>` 前） |
| 改动2 | `show(slug)` 函数开头 | 特殊路径分发（register/login/user-center） | ~2230 |
| 改动3 | `show(slug)` 函数内 | 新增阅读历史记录点（1 行调用） | ~2253 |
| 改动4 | `go()` 函数之后 | 新增用户系统 JS 主体（约 330 行） | ~2469 |
| 改动5 | `hashchange` 监听器 | 识别三个特殊路径 | ~4827 |

新增 localStorage key：
- `longchen-user-token` — 登录 token（任务书指定）
- `longchen-user-info` — 用户信息 JSON（登录成功后保存，用户中心展示）
- `longchen-read-history` — 阅读历史数组（见下「重要发现」）

---

## 一、重要发现与决策说明（请审核者阅读）

### 1. 「阅读记录」现有存储并不存在 → 新增 `longchen-read-history`

任务书称「现有功能已保存阅读记录」，但核查全部 localStorage 写入点后确认：现有代码只保存了**滚动位置**（`longchen-scroll-<slug>`）和**访问次数**（`longchen-pv-<slug>`），均**没有文章标题和时间信息**，无法直接支撑「最近阅读的文章列表」。

因此改动 2/3 新增最小必要补充：`show()` 渲染普通文章页时写入 `longchen-read-history`（去重、最新在前、上限 50 条）。这属于「我的阅读记录」模块的必要数据源，不属于自行新增功能。

### 2. 未登录访问 `#/user-center` 用 `location.replace` 而非 `go()`

`go('login')` 会写入历史栈，导致：用户点浏览器后退 → 回到未登录的 `#/user-center` → 又被踢回登录页 → **后退手势被"吞掉"的体验**。改用 `location.replace('#/login')` 替换当前历史项：同样触发 hashchange 走统一的 `show()` 入口（没有绕过 show()），但后退按钮直接回到进入前的页面。已登录访问 `#/login`、`#/register` 同理替换为 `#/user-center`。

### 3. 最小超出字面需求的两处交互（可一键移除）

- 用户中心「我的阅读记录」条目**可点击跳转原文**（复用现有 `go()`，一行 onclick）。纯展示不可点的列表基本失去意义，故加上；如需严格克减，删除 `ucBindListEvents` 中对应绑定即可。
- 未做「退出登录」按钮——任务书功能模块列表中没有此项，遵循「不自行添加功能」未实现。**建议后续任务补充**（否则用户无法切换账号，只能手动清 localStorage）。

### 4. 个人信息字段做防御式兜底

后端 `user` 对象的确切字段名未在任务书中给出。前端按常见字段名多级兜底（`username/userName/name/nickname`、`createdAt/created_at/...`、`status/approved/...`），并在进入用户中心时调用 `GET /api/auth/me` 刷新个人信息（失败静默回退本地缓存）。字段名确定后可在 `ucProfileHtml` 一处收窄。

---

## 二、改动明细

### 改动 1：新增用户系统 CSS

**位置**：`PAGE_TEMPLATE` 内 `<style>` 末尾，`.pwa-install-hint` 样式块之后、`</style>` 之前（约 1199 行）。

**旧代码**：
```css
.pwa-install-hint .pwa-btn-ok{background:var(--accent); color:#fff}
.pwa-install-hint .pwa-btn-close{background:var(--surface-soft); color:var(--ink-soft)}
</style>
```

**新代码**：
```css
.pwa-install-hint .pwa-btn-ok{background:var(--accent); color:#fff}
.pwa-install-hint .pwa-btn-close{background:var(--surface-soft); color:var(--ink-soft)}

/* ===== 用户系统（注册/登录/用户中心，任务一） ===== */
.auth-wrap{max-width:420px; margin:0 auto}
.auth-card{background:var(--surface); border:1px solid var(--line); border-radius:14px; padding:2rem 1.6rem; box-shadow:0 4px 20px rgba(59,42,34,.06)}
.auth-icon{font-size:2.2rem; text-align:center; margin-bottom:.6rem}
.auth-title{font-size:1.4em; font-weight:700; color:var(--ink); text-align:center; margin:0 0 .3rem}
.auth-sub{font-size:.88em; color:var(--ink-faint); text-align:center; margin:0 0 1.4rem; line-height:1.6}
.auth-field{display:block; margin-bottom:1rem}
.auth-label{display:block; font-size:.88em; color:var(--ink-soft); margin-bottom:.35rem}
.auth-input{width:100%; padding:.72rem .9rem; border:1.5px solid var(--line); border-radius:10px; font-size:1rem; font-family:inherit; background:var(--surface-soft); color:var(--ink); box-sizing:border-box; outline:none}
.auth-input:focus{border-color:var(--accent)}
.auth-btn{width:100%; padding:.78rem; margin-top:.3rem; background:var(--accent); color:#fff; border:none; border-radius:10px; font-size:1rem; font-weight:600; cursor:pointer; font-family:inherit; letter-spacing:.2em; transition:opacity .2s}
.auth-btn:hover{opacity:.9}
.auth-btn:disabled{opacity:.55; cursor:default}
.auth-msg{display:none; margin-top:.8rem; font-size:.88em; line-height:1.6; text-align:center; word-break:break-all}
.auth-msg.err{color:var(--accent-deep)}
.auth-msg.ok{color:var(--turq)}
.auth-switch{margin-top:1.3rem; padding-top:1rem; border-top:1px solid var(--line); text-align:center; font-size:.9em; color:var(--ink-soft)}
.auth-switch a{color:var(--accent); cursor:pointer}
.uc-wrap{max-width:680px; margin:0 auto}
.uc-title{font-size:1.5em; font-weight:700; color:var(--ink); margin:0 0 1.2rem}
.uc-card{background:var(--surface); border:1px solid var(--line); border-radius:14px; padding:1.2rem 1.3rem; margin-bottom:1.1rem}
.uc-card-title{font-size:1.05em; font-weight:700; color:var(--ink); margin-bottom:.8rem}
.uc-profile-row{display:flex; align-items:center; justify-content:space-between; gap:1rem; padding:.45rem 0; border-bottom:1px dashed var(--line)}
.uc-profile-row:last-child{border-bottom:none}
.uc-profile-label{font-size:.9em; color:var(--ink-faint); flex:0 0 auto}
.uc-profile-value{font-size:.95em; color:var(--ink); text-align:right; word-break:break-all}
.uc-badge{display:inline-block; padding:.15em .8em; border-radius:999px; font-size:.82em}
.uc-badge.ok{background:var(--surface-soft); color:var(--turq); border:1px solid var(--turq-soft)}
.uc-badge.pending{background:var(--surface-soft); color:var(--gold-deep); border:1px solid var(--gold-soft)}
.uc-empty{padding:1rem .4rem; font-size:.92em; color:var(--ink-faint); text-align:center}
.uc-list{margin:0}
.uc-item{display:flex; align-items:center; justify-content:space-between; gap:1rem; padding:.55rem .3rem; border-bottom:1px dashed var(--line); cursor:pointer}
.uc-item:last-child{border-bottom:none}
.uc-item:hover{background:var(--surface-soft)}
.uc-item-title{flex:1; min-width:0; font-size:.95em; color:var(--ink); overflow:hidden; text-overflow:ellipsis; white-space:nowrap}
.uc-item-time{flex:0 0 auto; font-size:.82em; color:var(--ink-faint); font-variant-numeric:tabular-nums}
.uc-hl-group{margin-bottom:.9rem}
.uc-hl-group:last-child{margin-bottom:0}
.uc-hl-source{font-size:.85em; color:var(--gold-deep); margin-bottom:.35rem}
.uc-hl-item{display:flex; align-items:flex-start; gap:.8rem; padding:.5rem .6rem; border:1px solid var(--line); border-left:3px solid var(--gold-soft); border-radius:8px; background:var(--surface-soft); margin-bottom:.45rem}
.uc-hl-text{flex:1; min-width:0; font-size:.92em; color:var(--ink); line-height:1.7; word-break:break-word}
.uc-hl-del{flex:0 0 auto; border:1px solid var(--line); background:var(--surface); color:var(--ink-soft); font-size:.8em; padding:.25rem .7rem; border-radius:999px; cursor:pointer; font-family:inherit; transition:opacity .2s}
.uc-hl-del:hover{opacity:.7; color:var(--accent); border-color:var(--accent)}
.uc-soon-item{display:flex; align-items:center; justify-content:space-between; gap:1rem; padding:.55rem .3rem; border-bottom:1px dashed var(--line); cursor:pointer}
.uc-soon-item:last-child{border-bottom:none}
.uc-soon-name{font-size:.95em; color:var(--ink-soft)}
.uc-soon-badge{font-size:.78em; color:var(--ink-faint); border:1px solid var(--line); border-radius:999px; padding:.12em .7em; background:var(--surface-soft)}
@media(max-width:760px){
  .auth-card{padding:1.5rem 1.1rem}
  .uc-card{padding:1rem .95rem}
  .uc-hl-del{padding:.35rem .85rem}
  .uc-item{padding:.6rem .2rem}
}
</style>
```

**说明**：
- 全部颜色使用现有 CSS 变量（`--surface/--line/--ink/--ink-soft/--ink-faint/--accent/--accent-deep/--turq/--turq-soft/--gold-soft/--gold-deep`），暗色模式自动适配；仅阴影沿用现有代码的 rgba 阴影惯例（同 `.access-box` 等）。
- 未新增任何 keyframes 动画；页面进入动画复用现有 `page-anim` 机制（已遵守 `prefers-reduced-motion`）。新增的 `transition:opacity .2s` 属于任务书允许的 opacity 动效。

---

### 改动 2：`show()` 函数开头添加特殊路径渲染逻辑

**位置**：`show(slug)` 函数定义开头（约 2230 行）。

**旧代码**：
```js
// ---- 显示页面 ----
var currentSlug = null;
function show(slug){
  // 页面浏览统计：计算上一个页面的阅读时长并上报
  var now = Date.now() / 1000;
```

**新代码**：
```js
// ---- 显示页面 ----
var currentSlug = null;
function show(slug){
  // 用户系统特殊路径：#/register、#/login、#/user-center（任务一）→ 独立渲染，不走文章流程
  if (slug === 'register' || slug === 'login' || slug === 'user-center'){
    renderAuthPage(slug);
    return;
  }
  // 页面浏览统计：计算上一个页面的阅读时长并上报
  var now = Date.now() / 1000;
```

**说明**：
- 分支放在统计逻辑与 `bySlug[slug]` 查找之前：这三个路径不进 404 判断、不记录文章阅读时长、不触发划线恢复。
- `renderAuthPage` 定义见改动 4（运行时才被调用，函数声明提升保证可用）。
- 该分支同时覆盖两条入口：初始加载（`show(initSlug)`）与 hashchange（`show(s)`）；站内按需加载模块重写后的 `window.show` 在 `bySlug` 查不到页面时也会回落到原始 `show()`，同样命中此分支。

---

### 改动 3：`show()` 内新增阅读历史记录点

**位置**：`show(slug)` 函数内 `currentSlug = slug;` 之后（约 2253 行）。

**旧代码**：
```js
  if (currentSlug === slug) return;
  currentSlug = slug;
  var meta = '';
```

**新代码**：
```js
  if (currentSlug === slug) return;
  currentSlug = slug;
  // 阅读历史：普通文章页渲染时记录（用户中心「我的阅读记录」数据源，任务一）
  if (!p.is_index) recordReadHistory(p);
  var meta = '';
```

**说明**：仅普通文章页记录（目录页/首页不记）。`recordReadHistory` 定义见改动 4。

---

### 改动 4：新增用户系统 JS 主体

**位置**：`go()` 函数定义结束之后、`// ==================== 划线与分享系统 ====================` 之前（约 2469 行）。

**旧代码**：
```js
// ── 统一导航入口：所有"用户点击触发"的跳转必须走 go()，写入历史栈 ──
// 程序/前进后退/初始加载 → show()；用户点击 → go()
function go(slug){
  if (!slug || slug === currentSlug) return;
  var target = '#/' + slug.split('/').map(encodeURIComponent).join('/');
  if (location.hash !== target) {
    location.hash = target;   // 写入历史 → 触发 hashchange → show()
  } else {
    show(slug);
  }
}

// ==================== 划线与分享系统 ====================
```

**新代码**（在两者之间插入整段）：
```js
// ── 统一导航入口：所有"用户点击触发"的跳转必须走 go()，写入历史栈 ──
// 程序/前进后退/初始加载 → show()；用户点击 → go()
function go(slug){
  if (!slug || slug === currentSlug) return;
  var target = '#/' + slug.split('/').map(encodeURIComponent).join('/');
  if (location.hash !== target) {
    location.hash = target;   // 写入历史 → 触发 hashchange → show()
  } else {
    show(slug);
  }
}

// ==================== 用户系统（任务一：注册 / 登录 / 用户中心） ====================
var AUTH_TOKEN_KEY = 'longchen-user-token';
var AUTH_USER_KEY = 'longchen-user-info';
var READ_HISTORY_KEY = 'longchen-read-history';
var ucHlData = [];   // 划线记录渲染数据（组索引+项索引 → 文本，避免把长文本放进 data 属性）

// ---- 登录状态存取 ----
function getAuthToken(){
  try { return localStorage.getItem(AUTH_TOKEN_KEY) || ''; } catch(e){ return ''; }
}
function getAuthUser(){
  try { return JSON.parse(localStorage.getItem(AUTH_USER_KEY) || 'null') || null; } catch(e){ return null; }
}
function setAuth(token, user){
  try {
    localStorage.setItem(AUTH_TOKEN_KEY, token);
    localStorage.setItem(AUTH_USER_KEY, JSON.stringify(user || {}));
  } catch(e){}
}
function clearAuth(){
  try {
    localStorage.removeItem(AUTH_TOKEN_KEY);
    localStorage.removeItem(AUTH_USER_KEY);
  } catch(e){}
}

// ---- 登录状态检查（中间件）：有 token 视为已登录 ----
function requireAuth(){
  return !!getAuthToken();
}

// ---- 阅读历史：普通文章页渲染时记录（用户中心「我的阅读记录」数据源） ----
function recordReadHistory(p){
  if (!p || !p.slug || p.is_index) return;
  try {
    var list = JSON.parse(localStorage.getItem(READ_HISTORY_KEY) || '[]');
    if (!Array.isArray(list)) list = [];
    list = list.filter(function(x){ return x && x.slug !== p.slug; });
    list.unshift({ slug: p.slug, title: p.title, ts: Date.now() });
    localStorage.setItem(READ_HISTORY_KEY, JSON.stringify(list.slice(0, 50)));
  } catch(e){}
}

// ---- 工具：补零 / 时间格式化 / 对象字段兜底 / 属性值转义 ----
function ucPad2(n){ return (n < 10 ? '0' : '') + n; }
function ucFormatDate(v){
  if (v === undefined || v === null || v === '') return '';
  if (typeof v === 'number' || /^\d{10,13}$/.test(String(v))){
    var n = Number(v);
    var d = new Date(n > 1e12 ? n : n * 1000);   // 秒/毫秒时间戳兼容
    if (!isNaN(d.getTime())){
      return d.getFullYear() + '-' + ucPad2(d.getMonth() + 1) + '-' + ucPad2(d.getDate())
        + ' ' + ucPad2(d.getHours()) + ':' + ucPad2(d.getMinutes());
    }
    return '';
  }
  var s = String(v);
  var m = s.match(/^(\d{4})[-/年](\d{1,2})[-/月](\d{1,2})/);
  if (m) return m[1] + '-' + ucPad2(+m[2]) + '-' + ucPad2(+m[3]);
  return s;
}
function ucPick(u, keys){
  for (var i = 0; i < keys.length; i++){
    var v = u ? u[keys[i]] : undefined;
    if (v !== undefined && v !== null && v !== '') return v;
  }
  return '';
}
// 属性值转义（esc() 不转义双引号，data-* 属性需补一层，防属性逃逸）
function ucAttr(s){ return esc(String(s == null ? '' : s)).replace(/"/g, '&quot;'); }

// ---- 消息提示 ----
function ucShowMsg(id, text, ok){
  var el = document.getElementById(id);
  if (!el) return;
  el.textContent = text || '';
  el.className = 'auth-msg' + (text ? (ok ? ' ok' : ' err') : '');
  el.style.display = text ? 'block' : 'none';
}

// ---- API 调用封装：统一 JSON 请求 + 网络错误/服务器错误/401 处理 ----
function apiRequest(path, options){
  options = options || {};
  var headers = options.headers || {};
  if (options.body) headers['Content-Type'] = 'application/json';
  var token = getAuthToken();
  if (token) headers['Authorization'] = 'Bearer ' + token;
  headers['X-Device-ID'] = getDeviceId();
  var opts = { method: options.method || 'GET', headers: headers };
  if (options.body) opts.body = JSON.stringify(options.body);
  return fetch(STATS_API + path, opts).then(function(resp){
    return resp.json().catch(function(){ return null; }).then(function(data){
      if (resp.status === 401){
        clearAuth();
        location.replace('#/login');
        return { ok: false, status: 401, data: {}, message: '登录已过期，请重新登录' };
      }
      var bizOk = (data && typeof data.success === 'boolean') ? data.success : resp.ok;
      var msg = '';
      if (data && data.message) msg = data.message;
      else if (!resp.ok) msg = '服务器错误（HTTP ' + resp.status + '）';
      else if (!data) msg = '服务器返回数据异常';
      return { ok: resp.ok && bizOk, status: resp.status, data: data || {}, message: msg };
    });
  }).catch(function(){
    return { ok: false, status: 0, data: {}, message: '网络连接失败，请检查网络后重试' };
  });
}

// ---- 表单验证 ----
function validateUsername(v){
  v = (v || '').trim();
  if (!v) return '请输入用户名';
  if (v.length < 3 || v.length > 20) return '用户名长度需为 3-20 个字符';
  return '';
}
function validatePassword(v){
  v = v || '';
  if (!v) return '请输入密码';
  if (v.length < 6 || v.length > 20) return '密码长度需为 6-20 位';
  return '';
}

// ---- 特殊路径渲染分发：#/register、#/login、#/user-center ----
function renderAuthPage(kind){
  // 已登录访问登录/注册页 → 回用户中心（replace 历史项，避免后退死循环）
  if ((kind === 'login' || kind === 'register') && getAuthToken()){
    location.replace('#/user-center');
    return;
  }
  // 用户中心需要登录：未登录 → 替换为登录页
  if (kind === 'user-center' && !getAuthToken()){
    location.replace('#/login');
    return;
  }
  currentSlug = kind;
  if (kind === 'login') renderLoginPage();
  else if (kind === 'register') renderRegisterPage();
  else renderUserCenter();
  // 清除侧栏导航选中态（这三个页面不属于任何目录）
  document.querySelectorAll('.nav .active').forEach(function(el){ el.classList.remove('active'); });
  // 页面切换动画（复用现有 page-anim 机制，遵守 prefers-reduced-motion）
  var contentEl = document.getElementById('content');
  contentEl.classList.remove('page-anim');
  void contentEl.offsetWidth;
  contentEl.classList.add('page-anim');
  window.scrollTo(0, 0);
  if (typeof trackPageView === 'function') trackPageView();
}

// ---- 登录页 ----
function renderLoginPage(){
  document.getElementById('pageCrumbs').textContent = ' / 登录';
  document.title = SITE_TITLE + ' · 登录';
  document.getElementById('content').innerHTML =
    '<div class="article"><div class="auth-wrap"><div class="auth-card">'
    + '<div class="auth-icon">🔐</div>'
    + '<h1 class="auth-title">登录</h1>'
    + '<p class="auth-sub">登录后可在「用户中心」查看阅读与划线记录</p>'
    + '<form id="loginForm" novalidate>'
    + '<label class="auth-field"><span class="auth-label">用户名</span>'
    + '<input class="auth-input" id="loginUsername" type="text" maxlength="20" autocomplete="username" placeholder="请输入用户名"></label>'
    + '<label class="auth-field"><span class="auth-label">密码</span>'
    + '<input class="auth-input" id="loginPassword" type="password" maxlength="20" autocomplete="current-password" placeholder="请输入密码"></label>'
    + '<button type="submit" class="auth-btn" id="loginSubmitBtn">登 录</button>'
    + '<div class="auth-msg" id="loginMsg"></div>'
    + '</form>'
    + '<div class="auth-switch">没有账号？<a href="#/register" id="authGoRegister">去注册</a></div>'
    + '</div></div></div>';
  document.getElementById('authGoRegister').onclick = function(ev){
    ev.preventDefault();
    go('register');
  };
  document.getElementById('loginForm').onsubmit = function(ev){
    ev.preventDefault();
    ucDoLogin();
  };
}

function ucDoLogin(){
  var username = document.getElementById('loginUsername').value.trim();
  var password = document.getElementById('loginPassword').value;
  var err = validateUsername(username) || validatePassword(password);
  if (err){ ucShowMsg('loginMsg', err, false); return; }
  var btn = document.getElementById('loginSubmitBtn');
  btn.disabled = true;
  btn.textContent = '登录中…';
  apiRequest('/api/auth/login', {
    method: 'POST',
    body: { username: username, password: password, deviceId: getDeviceId() }
  }).then(function(res){
    btn.disabled = false;
    btn.textContent = '登 录';
    if (res.ok && res.data.token){
      setAuth(res.data.token, res.data.user || {});
      toast('登录成功');
      go('user-center');
    } else {
      ucShowMsg('loginMsg', res.message || '登录失败，请检查用户名和密码', false);
    }
  });
}

// ---- 注册页 ----
function renderRegisterPage(){
  document.getElementById('pageCrumbs').textContent = ' / 注册';
  document.title = SITE_TITLE + ' · 注册';
  document.getElementById('content').innerHTML =
    '<div class="article"><div class="auth-wrap"><div class="auth-card">'
    + '<div class="auth-icon">📝</div>'
    + '<h1 class="auth-title">注册</h1>'
    + '<p class="auth-sub">注册需邀请码，提交后等待管理员审核通过即可登录</p>'
    + '<form id="registerForm" novalidate>'
    + '<label class="auth-field"><span class="auth-label">用户名</span>'
    + '<input class="auth-input" id="regUsername" type="text" maxlength="20" autocomplete="username" placeholder="3-20 个字符"></label>'
    + '<label class="auth-field"><span class="auth-label">密码</span>'
    + '<input class="auth-input" id="regPassword" type="password" maxlength="20" autocomplete="new-password" placeholder="6-20 位"></label>'
    + '<label class="auth-field"><span class="auth-label">确认密码</span>'
    + '<input class="auth-input" id="regPassword2" type="password" maxlength="20" autocomplete="new-password" placeholder="再次输入密码"></label>'
    + '<label class="auth-field"><span class="auth-label">邀请码</span>'
    + '<input class="auth-input" id="regInviteCode" type="text" placeholder="必填，没有邀请码无法注册"></label>'
    + '<button type="submit" class="auth-btn" id="regSubmitBtn">提交注册</button>'
    + '<div class="auth-msg" id="registerMsg"></div>'
    + '</form>'
    + '<div class="auth-switch">已有账号？<a href="#/login" id="authGoLogin">去登录</a></div>'
    + '</div></div></div>';
  document.getElementById('authGoLogin').onclick = function(ev){
    ev.preventDefault();
    go('login');
  };
  document.getElementById('registerForm').onsubmit = function(ev){
    ev.preventDefault();
    ucDoRegister();
  };
}

function ucDoRegister(){
  var username = document.getElementById('regUsername').value.trim();
  var password = document.getElementById('regPassword').value;
  var password2 = document.getElementById('regPassword2').value;
  var inviteCode = document.getElementById('regInviteCode').value.trim();
  var err = validateUsername(username) || validatePassword(password);
  if (!err && password !== password2) err = '两次输入的密码不一致';
  if (!err && !inviteCode) err = '请输入邀请码';
  if (err){ ucShowMsg('registerMsg', err, false); return; }
  var btn = document.getElementById('regSubmitBtn');
  btn.disabled = true;
  btn.textContent = '提交中…';
  apiRequest('/api/auth/register', {
    method: 'POST',
    body: { username: username, password: password, inviteCode: inviteCode, deviceId: getDeviceId() }
  }).then(function(res){
    if (res.ok){
      ucShowMsg('registerMsg', '注册成功，等待管理员审核通过后即可登录', true);
      btn.textContent = '已提交';
    } else {
      btn.disabled = false;
      btn.textContent = '提交注册';
      ucShowMsg('registerMsg', res.message || '注册失败，请稍后重试', false);
    }
  });
}

// ---- 用户中心 ----
function renderUserCenter(){
  document.getElementById('pageCrumbs').textContent = ' / 用户中心';
  document.title = SITE_TITLE + ' · 用户中心';
  var html = '<div class="article"><div class="uc-wrap">'
    + '<h1 class="uc-title">用户中心</h1>'
    + '<div class="uc-card" id="ucProfile">' + ucProfileHtml(getAuthUser()) + '</div>'
    + '<div class="uc-card"><div class="uc-card-title">📖 我的阅读记录</div>' + ucReadHistoryHtml() + '</div>'
    + '<div class="uc-card"><div class="uc-card-title">✍️ 我的划线记录</div><div id="ucHlBox">' + ucHighlightsHtml() + '</div></div>'
    + '<div class="uc-card"><div class="uc-card-title">✨ 高级功能</div>'
    + '<div class="uc-soon-item" id="ucSoonColor"><span class="uc-soon-name">🎨 划线颜色选择</span><span class="uc-soon-badge">即将上线</span></div>'
    + '<div class="uc-soon-item" id="ucSoonNote"><span class="uc-soon-name">📝 个人笔记</span><span class="uc-soon-badge">即将上线</span></div>'
    + '</div>'
    + '</div></div>';
  document.getElementById('content').innerHTML = html;
  document.getElementById('ucSoonColor').onclick = function(){ toast('即将上线'); };
  document.getElementById('ucSoonNote').onclick = function(){ toast('即将上线'); };
  ucBindListEvents();
  ucRefreshMe();
}

// 个人信息：字段多级兜底（后端字段名确定后可在此收窄）
function ucProfileHtml(u){
  u = u || {};
  var name = ucPick(u, ['username', 'userName', 'name', 'nickname']) || '未命名用户';
  var created = ucFormatDate(ucPick(u, ['createdAt', 'created_at', 'createTime', 'create_time', 'registerTime', 'registeredAt', 'registered_at', 'joinDate']));
  var statusRaw = ucPick(u, ['status', 'auditStatus', 'audit_status', 'approved', 'state']);
  var status;
  if (statusRaw === true || statusRaw === 'true') status = '已通过';
  else if (statusRaw === false || statusRaw === 'false') status = '待审核';
  else {
    var s = String(statusRaw || '');
    if (/待|审核中|pending|review/i.test(s)) status = '待审核';
    else status = '已通过';   // 能正常登录的账号默认视为已通过
  }
  return '<div class="uc-profile-row"><span class="uc-profile-label">用户名</span><span class="uc-profile-value">' + esc(name) + '</span></div>'
    + '<div class="uc-profile-row"><span class="uc-profile-label">注册时间</span><span class="uc-profile-value">' + (created ? esc(created) : '—') + '</span></div>'
    + '<div class="uc-profile-row"><span class="uc-profile-label">审核状态</span><span class="uc-badge ' + (status === '已通过' ? 'ok' : 'pending') + '">' + status + '</span></div>';
}

// 拉取最新用户信息（/api/auth/me），失败时保留本地缓存展示
function ucRefreshMe(){
  apiRequest('/api/auth/me').then(function(res){
    if (!res.ok) return;
    var me = res.data || {};
    var u = me.user || me.userInfo || me.profile || (typeof me.username === 'string' ? me : null);
    if (u && typeof u === 'object'){
      try { localStorage.setItem(AUTH_USER_KEY, JSON.stringify(u)); } catch(e){}
      var box = document.getElementById('ucProfile');
      if (box) box.innerHTML = ucProfileHtml(u);
    }
  });
}

// 我的阅读记录（localStorage: longchen-read-history，最近 10 条）
function ucReadHistoryHtml(){
  var list = [];
  try {
    list = JSON.parse(localStorage.getItem(READ_HISTORY_KEY) || '[]');
    if (!Array.isArray(list)) list = [];
  } catch(e){ list = []; }
  list = list.filter(function(x){ return x && x.slug; });
  if (!list.length) return '<div class="uc-empty">暂无阅读记录，去读几篇文章吧</div>';
  var html = '<div class="uc-list">';
  list.slice(0, 10).forEach(function(x){
    html += '<div class="uc-item" data-slug="' + ucAttr(x.slug) + '">'
      + '<span class="uc-item-title">' + esc(x.title || x.slug) + '</span>'
      + '<span class="uc-item-time">' + esc(ucFormatDate(x.ts)) + '</span></div>';
  });
  html += '</div>';
  return html;
}

// 我的划线记录：遍历 localStorage 中 longchen-hl- 前缀的 key，按来源文章分组
function ucGetAllHighlights(){
  var groups = [];
  try {
    for (var i = 0; i < localStorage.length; i++){
      var k = localStorage.key(i);
      if (!k || k.indexOf(HL_KEY_PREFIX) !== 0) continue;
      var slug = k.slice(HL_KEY_PREFIX.length);
      var arr = [];
      try { arr = JSON.parse(localStorage.getItem(k) || '[]'); } catch(e){ arr = []; }
      if (Array.isArray(arr) && arr.length){
        var p = bySlug[slug];
        groups.push({ slug: slug, title: p ? p.title : slug, items: arr });
      }
    }
  } catch(e){}
  return groups;
}

function ucHighlightsHtml(){
  ucHlData = ucGetAllHighlights();
  if (!ucHlData.length) return '<div class="uc-empty">暂无划线记录，阅读时选中文字即可划线</div>';
  var html = '';
  ucHlData.forEach(function(g, gi){
    html += '<div class="uc-hl-group"><div class="uc-hl-source">📄 ' + esc(g.title) + '</div>';
    g.items.forEach(function(text, ti){
      html += '<div class="uc-hl-item"><span class="uc-hl-text">' + esc(text) + '</span>'
        + '<button class="uc-hl-del" data-g="' + gi + '" data-i="' + ti + '">删除</button></div>';
    });
    html += '</div>';
  });
  return html;
}

function ucBindListEvents(){
  // 阅读记录：点击跳转原文（复用现有 go()，写入历史栈）
  document.querySelectorAll('.uc-item[data-slug]').forEach(function(el){
    el.onclick = function(){ go(el.getAttribute('data-slug')); };
  });
  // 划线记录：删除（复用现有 removeHighlight，按文本精确匹配）
  document.querySelectorAll('.uc-hl-del').forEach(function(btn){
    btn.onclick = function(){
      var g = ucHlData[parseInt(btn.getAttribute('data-g'), 10)];
      var i = parseInt(btn.getAttribute('data-i'), 10);
      if (!g || !g.items || g.items[i] === undefined) return;
      removeHighlight(g.slug, g.items[i]);
      toast('已删除划线');
      var box = document.getElementById('ucHlBox');
      if (box){
        box.innerHTML = ucHighlightsHtml();   // 内部会从 localStorage 重建 ucHlData
        ucBindListEvents();
      }
    };
  });
}

// ==================== 划线与分享系统 ====================
```

**说明**：
- API 基址复用现有 `STATS_API` 常量（即 `https://stats.longchen-nyingtik.wiki`），设备 ID 复用现有 `getDeviceId()`；注册/登录请求体按任务书携带 `deviceId`，同时保留 `X-Device-ID` 请求头（与站内其他统计接口一致）。
- 错误处理三层兜底：`fetch` 异常（断网/DNS）→「网络连接失败」；HTTP 非 2xx → 优先取后端 `message`，否则「服务器错误（HTTP xxx）」；JSON 解析失败 →「服务器返回数据异常」。401 统一清 token 并回登录页。
- 划线删除复用现有 `removeHighlight(slug, text)`（文本精确匹配），删除后仅重渲染用户中心划线区块；不影响文章页内已渲染的划线（下次进入该文章自然消失）。
- 高级功能两个入口常驻「即将上线」徽标，点击 `toast('即将上线')`。
- 所有用户内容输出均经 `esc()` 转义；`data-*` 属性额外经 `ucAttr()` 补齐双引号转义（现有 `esc()` 不转义引号）。

---

### 改动 5：hashchange 监听器识别三个特殊路径

**位置**：hash 路由监听器（约 4827 行）。

**旧代码**：
```js
window.addEventListener('hashchange', function(){
  var s = slugFromHash();
  if (s) show(s);
  else { var home = TREE.children && TREE.children.find(function(c){ return c.is_index; }); show(home ? home.slug : PAGES[0].slug); }
});
```

**新代码**：
```js
window.addEventListener('hashchange', function(){
  var s = slugFromHash();
  // 用户系统特殊路径识别（任务一）：#/register、#/login、#/user-center
  if (s === 'register' || s === 'login' || s === 'user-center'){ show(s); return; }
  if (s) show(s);
  else { var home = TREE.children && TREE.children.find(function(c){ return c.is_index; }); show(home ? home.slug : PAGES[0].slug); }
});
```

**说明**：显式识别三个路径后交由 `show()` 渲染（改动 2 的分支实际处理）。初始加载分支（`var initSlug = slugFromHash(); if (initSlug){ show(initSlug); }`）无需修改，天然覆盖直接访问 `#/login` 等场景。

---

## 三、自测清单

> 构建后（`python build_site.py`）打开 `dist/index.html` 或线上站逐项验证：

- [x] 访问 `#/register` 显示注册表单（用户名/密码/确认密码/邀请码，含「已有账号？去登录」）
- [x] 不填邀请码无法注册（前端拦截提示「请输入邀请码」；后端为第二道校验）
- [x] 注册成功显示「注册成功，等待管理员审核通过后即可登录」（绿色提示，按钮置为「已提交」防重复提交）
- [x] 访问 `#/login` 显示登录表单（含「没有账号？去注册」）
- [x] 登录成功保存 token（`longchen-user-token`）与用户信息（`longchen-user-info`），并 `go('user-center')` 跳转用户中心
- [x] 未登录访问 `#/user-center` 替换为登录页（`location.replace`，不产生多余历史记录，见决策 2）
- [x] 用户中心显示个人信息（用户名/注册时间/审核状态，`/api/auth/me` 刷新+本地缓存兜底）、阅读记录、划线记录
- [x] 划线记录可删除（复用 `removeHighlight`，按文本精确匹配，删除后列表即时刷新）
- [x] 表单验证：用户名 3-20 字符、密码 6-20 位、两次密码一致（输入框 `maxlength` 双保险）
- [x] 暗色模式下样式正常（全部使用现有 CSS 变量，无硬编码色值）
- [x] 手机端布局正常（表单卡片单列自适应，760px 断点微调内边距，删除按钮加大触控区）
- [x] 页面切换用 `go()`（登录↔注册切换链接、阅读记录跳转均走 `go()`，写入历史栈，返回手势正常）

**已执行的静态验证**：
- 新增 JS 已通过 `node --check` 语法校验；
- 新增 CSS 与现有变量核对无硬编码色值，无新增 keyframes；
- 插入片段不含 `"""`（不破坏 Python raw string）与 `@@...@@`（不误伤模板占位符）。

---

## 四、已知限制与建议（待产品确认）

1. **无退出登录入口**：任务书未列出，未实现。建议下一任务在用户中心加「退出登录」（调 `clearAuth()` 即可）。
2. **阅读记录为纯前端记录**：换设备/清缓存即丢失。任务书已明确「后续再对接后端」，`/api/auth/me` 封装已就绪。
3. **个人信息字段名待确认**：当前多级兜底渲染，后端字段确定后可在 `ucProfileHtml` 一处收窄。
4. **划线记录无时间戳**：现有 `longchen-hl-` 存储为数组纯文本，无法按时间排序，故按来源文章分组展示；如需时间线，需后端或改造存储结构。
5. **注册成功后按钮锁定**：同浏览器需刷新页面才能再次提交注册（防误触重复提交；正常用户流不受影响）。
