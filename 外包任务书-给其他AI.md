# 龙的传人网站 · 外包任务书（给其他AI用）

> 致执行本任务的AI助手：请严格按照以下要求执行。完成后把所有代码改动整理成MD格式返回，不要直接修改任何文件。
> 网站：https://longchen-nyingtik.wiki/
> 技术栈：自研零依赖Python构建器（build_site.py，约5000行），生成单文件HTML SPA，hash路由，无框架。
> 构建命令：`python build_site.py`，输出到 `dist/index.html`

---

## 通用约束（所有任务都必须遵守）

1. **只修改 build_site.py 一个文件**，不碰 sw.js、manifest.json、音频文件、页面内容数据。
2. **所有颜色用现有CSS变量**（var(--bg)、var(--surface)、var(--accent)、var(--line)、var(--ink)、var(--ink-soft)、var(--ink-faint)、var(--gold)等），禁止硬编码色值（暗色模式依赖变量自动切换）。
3. **动效只允许动画 transform 和 opacity**（GPU加速），时长0.2-0.3s，遵守 `prefers-reduced-motion`。
4. **改动前后站点功能必须完全正常**（音频播放、锁屏控制、AI搜索、主题切换、返回手势）。
5. **不要自行添加任何新功能**，只做本任务书列出的修改。
6. **交付格式**：把所有代码改动整理成MD格式，包含：
   - 每个改动的位置（函数名/大约行号）
   - 旧代码 → 新代码的完整对比
   - 改动说明
   - 自测清单（你认为需要测试的点）

---

## 任务一：用户系统前端开发（优先级最高）

### 背景
后端API已完成并部署，现在需要前端页面。统计Worker地址：https://stats.longchen-nyingtik.wiki

### 后端API说明

#### 注册接口
- **URL**: `POST /api/auth/register`
- **请求体**:
  ```json
  {
    "username": "用户名",
    "password": "密码",
    "inviteCode": "邀请码",
    "deviceId": "设备ID（从localStorage获取，key为longchen-device-id）"
  }
  ```
- **返回**: `{success: true, message: "注册成功，等待管理员审核"}` 或 `{success: false, message: "错误信息"}`

#### 登录接口
- **URL**: `POST /api/auth/login`
- **请求体**:
  ```json
  {
    "username": "用户名",
    "password": "密码",
    "deviceId": "设备ID"
  }
  ```
- **返回**: `{success: true, token: "JWT token", user: {...}}` 或 `{success: false, message: "错误信息"}`

#### 用户信息接口
- **URL**: `GET /api/auth/me`
- **Header**: `Authorization: Bearer <token>`
- **返回**: 用户信息、阅读记录、划线记录等

### 前端页面需求

#### 1. 注册页面（不直接展示，通过单独链接访问）
- 访问路径：`#/register`
- 表单字段：用户名、密码、确认密码、邀请码
- 邀请码必填，没有邀请码无法注册
- 注册成功后提示："注册成功，等待管理员审核通过后即可登录"
- 不需要邮箱验证

#### 2. 登录页面
- 访问路径：`#/login`
- 表单字段：用户名、密码
- 登录成功后保存token到localStorage（key: `longchen-user-token`）
- 登录失败显示错误信息

#### 3. 用户中心页面
- 访问路径：`#/user-center`
- 需要登录才能访问，未登录跳转到登录页
- 功能模块：
  - 个人信息（用户名、注册时间、审核状态）
  - 我的阅读记录（最近阅读的文章列表）
  - 我的划线记录（划线的内容列表，支持删除）
  - 高级功能（划线颜色选择、个人笔记——暂留入口，功能后续开发）

### 技术要求
1. 注册/登录页面样式与网站整体风格一致（宣纸底、藏红、金配色）
2. 表单验证：用户名3-20字符、密码6-20字符、两次密码一致
3. 登录状态持久化（localStorage存token）
4. 页面切换用现有的 `go()` 函数（不要用 `show()`）
5. 用户中心的阅读记录和划线记录，先从localStorage读取（现有功能已保存），后续再对接后端

### 交付
- 注册页面、登录页面、用户中心页面的完整HTML/CSS/JS代码
- 路由处理逻辑（hashchange中识别 #/register、#/login、#/user-center）
- 登录状态检查逻辑

---

## 任务二：AI问答体验优化

### 背景
用户反馈AI搜索存在以下问题：
1. 模糊匹配能力不足（输入"学号救命稻草"搜不到"唯一的救命稻草：选好上师"）
2. 引用来源显示数字但不列出对应文章
3. AI回复语气过于"佛里佛气"，需要生活化、温暖平和
4. 回复有时显示不全就停止了

### 需求

#### 1. 增强模糊匹配
- 在现有检索逻辑基础上，增加：
  - 拼音首字母匹配（如"xsm"匹配"选上师"）
  - 同义词扩展（如"救命"="依靠"="依怙"）
  - 部分匹配（输入3个字以上，只要标题或内容包含其中2个字就算匹配）
  - 错别字容错（编辑距离≤2算匹配）
- 匹配结果按相关度排序，最相关的排前面

#### 2. 引用来源优化
- AI回复中引用的数字[1][2][3]，在回复末尾列出对应的文章标题
- 每个引用可点击，点击跳转到对应文章
- 跳转后AI搜索面板最小化为左上角悬浮按钮（现有功能）

#### 3. AI回复语气调整
- 修改system prompt，要求：
  - 用生活化的语言，不要用佛经术语堆砌
  - 温暖平和，有智慧见地
  - 给人希望和信心
  - 不要开头就"阿弥陀佛"、"善哉善哉"之类的
  - 可以用比喻、故事来说明道理
- 示例：
  - ❌ "诸法无常，诸行无我，当勤精进，勿放逸。"
  - ✅ "其实我们每个人都在变化中，今天的你和昨天的你已经不一样了。接受这种变化，反而能让心安定下来。"

#### 4. 回复长度控制
- 检查API调用参数，确保max_tokens足够（至少2000）
- 如果回复被截断，在末尾显示"（回复较长，已截断，可细化问题获取完整回答）"

### 技术要求
- 修改 build_site.py 中的 searchKnowledge 函数（模糊匹配）
- 修改 sendAiAsk 函数（system prompt、引用处理）
- 不改变现有API调用方式（仍然走 https://ai.longchen-nyingtik.wiki）

### 交付
- 模糊匹配增强的完整代码
- 引用来源显示的完整代码
- 新的system prompt
- 回复截断处理代码

---

## 任务三：排版统一（可选，需确认后再做）

### 背景
现有首页排版已经比较统一，但任务书要求进一步规范。

### 需求（如果确认要做）
1. 首页各板块套用统一卡片容器（圆角14px、边框、内边距16px）
2. 目录树可点击行min-height:44px，行间用border-bottom分隔
3. 首屏顺序调整：最近更新（限5条）→ 四宫格快捷入口（2×2网格：上师开示/法音收听/书籍/AI问答）→ 完整目录 → 关于本站
4. 音频专辑区改为自适应网格
5. 页面左右留白：移动端16px，≥768px 24px

### 注意
- 这个任务改动较大，可能影响现有设计风格
- 建议先确认是否需要做，再开始执行

---

## 自测清单（所有任务完成后都要检查）

- [ ] 音频播放正常，锁屏控制正常
- [ ] AI搜索正常，能返回结果
- [ ] 主题切换（亮色/暗色）正常，新元素颜色正确
- [ ] 返回手势正常（左滑返回上一级，首页左滑退出）
- [ ] 页面切换动画正常
- [ ] 首页体积不超过500KB
- [ ] 手机端（375px宽）布局正常，不超出屏幕
- [ ] 暗色模式下无白块、无刺眼边框

---

## 重要提醒

1. **不要直接修改文件**，把所有代码改动整理成MD格式返回
2. **不要添加任务书以外的功能**
3. **遇到与现有代码冲突的情况，停下来说明，不要自行发挥**
4. **代码要写注释**，方便后续维护
5. **完成后列出所有改动点和自测结果**
