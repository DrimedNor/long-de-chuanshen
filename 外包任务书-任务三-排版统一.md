# 任务三：排版统一（可选，需确认后再做）

> 致执行本任务的AI助手：请严格按照以下要求执行。完成后把所有代码改动整理成MD格式返回，不要直接修改任何文件。
> 网站：https://longchen-nyingtik.wiki/
> 技术栈：自研零依赖Python构建器（build_site.py，约5000行），生成单文件HTML SPA，hash路由，无框架。

---

## ⚠️ 重要提醒

本任务改动较大，可能影响现有设计风格。执行前请确认用户确实需要做这个改版。如果现有排版已经满意，可以跳过本任务。

---

## 通用约束

1. **只修改 build_site.py 一个文件**，不碰其他文件。
2. **所有颜色用现有CSS变量**，禁止硬编码色值。
3. **不要自行添加任何新功能**，只做本任务书列出的修改。
4. **交付格式**：把所有代码改动整理成MD格式，包含：
   - 每个改动的位置（函数名/大约行号）
   - 旧代码 → 新代码的完整对比
   - 改动说明
   - 自测清单

---

## 背景

现有首页排版已经比较统一（有.hn-sec类），但任务书要求进一步规范，提升视觉一致性和首屏信息密度。

现有首页结构（renderHomeNav函数附近）：
1. 欢迎区（网站标题+副标题）
2. 最近更新
3. 上师开示目录
4. 龙钦宁提传承
5. 音频资料
6. 书籍
7. 关于本站
8. AI问答

---

## 需求一：统一板块卡片

### 现有问题
各板块的容器样式不完全统一，间距和圆角有差异。

### 改进方案
所有首页板块（最近更新、上师开示、传承、音频资料、书籍、关于本站、AI问答）套用统一卡片容器：

```css
.section-card{
  background:var(--surface);
  border:1px solid var(--line);
  border-radius:14px;
  padding:16px;
  margin:0 0 16px;
}
.section-card > .sec-head{
  display:flex;
  align-items:center;
  gap:.5rem;
  padding-bottom:10px;
  margin-bottom:12px;
  border-bottom:1px solid var(--line);
  font-weight:700;
  color:var(--ink);
}
.sec-head .sec-more{
  margin-left:auto;
  font-size:.85rem;
  color:var(--accent);
  font-weight:400;
  cursor:pointer;
}
```

板块标题结构统一为：
```html
<div class="sec-head">📖 上师开示 <span class="sec-more">查看全部 ›</span></div>
```

### 注意
- 现有.hn-sec类可以保留，也可以替换为.section-card
- 如果替换，要确保所有引用.hn-sec的地方都更新
- 板块标题的图标和文字保持现有内容不变

---

## 需求二：目录树规格统一

### 改进方案
1. 所有可点击行 `min-height:44px`（触控标准）
2. 行间用 `border-bottom:1px solid var(--line)` 分隔（最后一行无线）
3. 一级分类（如"1. 信心之源"）加粗
4. 分类描述（如"弄明白为什么出发"）用 `var(--ink-soft)` 小字号
5. 文章计数徽标（如"3篇"）右对齐，用 `var(--ink-faint)` 色
6. 目录树默认折叠到一级（现状是全展开）
7. 展开状态记忆到localStorage（key: `longchen-dir-expand-state`）

### 现有代码位置
- 目录树渲染：搜索"renderFullDirTree"或"nav-sec"
- 目录展开/折叠：搜索"nav-sec-head"或"toggle"

---

## 需求三：首页信息分层（降低首屏密度）

### 改进方案
首屏顺序调整为：
1. **最近更新**（限5条，不要太多）
2. **四宫格快捷入口**（2×2网格）：
   - 上师开示（图标📖，描述"10大分类，251篇开示"）
   - 法音收听（图标🎧，描述"74个音频，在线收听"）
   - 书籍（图标📚，描述"传承著作，深入学习"）
   - AI问答（图标🤖，描述"智能检索，有问必答"）
3. **完整目录**（默认折叠，点击展开）
4. **关于本站**

### 四宫格样式
```css
.quick-grid{
  display:grid;
  grid-template-columns:1fr 1fr;
  gap:12px;
  margin-bottom:16px;
}
.quick-grid .q-card{
  display:flex;
  flex-direction:column;
  gap:.3rem;
  align-items:flex-start;
  padding:16px;
  border-radius:14px;
  background:var(--surface);
  border:1px solid var(--line);
  cursor:pointer;
  transition:all .15s;
}
.quick-grid .q-card:hover{
  border-color:var(--accent);
  transform:translateY(-2px);
}
.quick-grid .q-card:active{
  transform:scale(.97);
}
.q-card .q-icon{font-size:1.5rem}
.q-card .q-title{font-weight:700;color:var(--ink)}
.q-card .q-desc{font-size:.8rem;color:var(--ink-soft)}
```

### 技术实现
- 在renderHomeNav函数中添加四宫格HTML
- 每个卡片绑定onclick，调用go()跳转到对应目录
- 完整目录部分默认折叠，点击"展开全部目录"按钮展开

---

## 需求四：音频专辑区优化

### 改进方案
1. 专辑卡片改为自适应网格：
   ```css
   .audio-grid{
     display:grid;
     grid-template-columns:repeat(auto-fill, minmax(150px, 1fr));
     gap:12px;
   }
   ```
2. 专辑内曲目列表默认收起，点击专辑展开
3. 正在播放的曲目用3px `var(--turq)` 左边框 + ▶ 图标共同标示（不能只用颜色）
4. 专辑卡片样式统一（圆角、边框、内边距）

### 现有代码位置
- 音频专辑渲染：搜索"renderAudioListByFolder"或"audio-group"
- 播放状态：搜索"curIdx"或"updatePlayBtns"

---

## 需求五：全局排版节奏

### 改进方案
1. 页面左右留白：移动端16px，≥768px 24px
2. 板块间距统一16px（卡片的margin-bottom）
3. 标题阶梯：
   - 文章h1 ≤ 1.5rem
   - h2 = 1.25rem
   - h3 = 1.1rem
   - 正文保持1rem/1.9不变
4. 分割线一律用 `var(--line)`，禁用深色粗线

### 检查点
- 搜索所有硬编码的padding/margin，确保统一
- 搜索所有font-size，确保标题阶梯一致
- 搜索所有border-color，确保用变量

---

## 现有相关代码位置（供参考）

- 首页渲染：搜索"renderHomeNav"或"isHome"
- 最近更新：搜索"HOME_UPDATE"或"home-update"
- 上师开示目录：搜索"hn-sec"或"renderFullDirTree"
- 音频资料：搜索"renderAudioListByFolder"
- 书籍部分：搜索"书籍"或"book"
- 关于本站：搜索"关于本站"或"about"
- AI问答：搜索"ai-ask-sec"或"AI 问答"
- CSS样式：文件开头的<style>标签内

---

## 交付内容

1. 统一板块卡片的CSS和HTML改动
2. 目录树规格统一的代码
3. 四宫格快捷入口的HTML/CSS/JS
4. 完整目录默认折叠的逻辑
5. 音频专辑网格优化
6. 全局排版节奏调整
7. localStorage展开状态记忆

---

## 自测清单

- [ ] 首页各板块卡片风格完全一致（圆角、边框、内边距、标题样式）
- [ ] 首屏375px宽（iPhone SE）下能看到"最近更新 + 四宫格入口"
- [ ] 四宫格点击能跳转到对应目录
- [ ] 完整目录默认折叠，点击展开
- [ ] 目录展开状态刷新后记住
- [ ] 目录树行高44px，行间有分隔线
- [ ] 音频专辑网格在375/768/1200三档宽度整齐
- [ ] 正在播放的曲目有左边框+▶图标
- [ ] 暗色模式下所有新元素颜色正常（无白块、无刺眼边框）
- [ ] 对比度抽查：新增文字全部达标
- [ ] 页面左右留白：手机16px，桌面24px
- [ ] 不影响音频播放、AI搜索、返回手势等现有功能
- [ ] 首页体积不超过500KB

---

## 风险提示

1. 本任务改动较大，可能影响现有设计风格
2. 四宫格快捷入口会改变首页结构，需要用户确认是否喜欢
3. 目录默认折叠可能影响用户浏览习惯（现有是全展开）
4. 建议先做一个版本，用户确认后再正式部署
