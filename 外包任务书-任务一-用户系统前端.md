# 任务一：用户系统前端开发

> 致执行本任务的AI助手：请严格按照以下要求执行。完成后把所有代码改动整理成MD格式返回，不要直接修改任何文件。
> 网站：https://longchen-nyingtik.wiki/
> 技术栈：自研零依赖Python构建器（build_site.py，约5000行），生成单文件HTML SPA，hash路由，无框架。

---

## 通用约束

1. **只修改 build_site.py 一个文件**，不碰其他文件。
2. **所有颜色用现有CSS变量**（var(--bg)、var(--surface)、var(--accent)、var(--line)、var(--ink)、var(--ink-soft)、var(--ink-faint)、var(--gold)等），禁止硬编码色值。
3. **动效只允许动画 transform 和 opacity**，时长0.2-0.3s，遵守 `prefers-reduced-motion`。
4. **不要自行添加任何新功能**，只做本任务书列出的修改。
5. **交付格式**：把所有代码改动整理成MD格式，包含：
   - 每个改动的位置（函数名/大约行号）
   - 旧代码 → 新代码的完整对比
   - 改动说明
   - 自测清单

---

## 背景

后端API已完成并部署，现在需要前端页面。统计Worker地址：https://stats.longchen-nyingtik.wiki

现有代码结构：
- `show(slug)` 函数：渲染页面，所有页面都走这个函数
- `go(slug)` 函数：用户点击触发的导航，写入历史栈
- hashchange 监听器：监听URL变化，调用show()
- 设备ID保存在localStorage，key为 `longchen-device-id`

---

## 后端API说明

### 注册接口
- **URL**: `POST https://stats.longchen-nyingtik.wiki/api/auth/register`
- **请求体**:
  ```json
  {
    "username": "用户名",
    "password": "密码",
    "inviteCode": "邀请码",
    "deviceId": "设备ID"
  }
  ```
- **返回**: `{success: true, message: "注册成功，等待管理员审核"}` 或 `{success: false, message: "错误信息"}`

### 登录接口
- **URL**: `POST https://stats.longchen-nyingtik.wiki/api/auth/login`
- **请求体**:
  ```json
  {
    "username": "用户名",
    "password": "密码",
    "deviceId": "设备ID"
  }
  ```
- **返回**: `{success: true, token: "JWT token", user: {...}}` 或 `{success: false, message: "错误信息"}`

### 用户信息接口
- **URL**: `GET https://stats.longchen-nyingtik.wiki/api/auth/me`
- **Header**: `Authorization: Bearer <token>`
- **返回**: 用户信息、阅读记录、划线记录等

---

## 前端页面需求

### 1. 注册页面（不直接展示，通过单独链接访问）
- 访问路径：`#/register`
- 表单字段：用户名、密码、确认密码、邀请码
- 邀请码必填，没有邀请码无法注册
- 注册成功后提示："注册成功，等待管理员审核通过后即可登录"
- 不需要邮箱验证
- 页面底部加一个"已有账号？去登录"的链接

### 2. 登录页面
- 访问路径：`#/login`
- 表单字段：用户名、密码
- 登录成功后保存token到localStorage（key: `longchen-user-token`）
- 登录失败显示错误信息
- 页面底部加一个"没有账号？去注册"的链接

### 3. 用户中心页面
- 访问路径：`#/user-center`
- 需要登录才能访问，未登录跳转到登录页
- 功能模块：
  - **个人信息**：用户名、注册时间、审核状态（待审核/已通过）
  - **我的阅读记录**：最近阅读的文章列表（从localStorage读取，现有功能已保存阅读记录）
  - **我的划线记录**：划线的内容列表（从localStorage读取，现有功能已保存划线，key前缀为`longchen-hl-`），支持删除
  - **高级功能**：划线颜色选择、个人笔记——暂留入口，显示"即将上线"

---

## 技术要求

1. 注册/登录页面样式与网站整体风格一致（宣纸底、藏红、金配色）
2. 表单验证：用户名3-20字符、密码6-20字符、两次密码一致
3. 登录状态持久化（localStorage存token和用户信息）
4. **页面切换用现有的 `go()` 函数**（不要用 `show()`）
5. 在hashchange监听器中添加对 `#/register`、`#/login`、`#/user-center` 的识别
6. 在show()函数中添加对这三个特殊路径的渲染逻辑
7. 用户中心的阅读记录和划线记录，先从localStorage读取，后续再对接后端
8. 表单提交用fetch，错误处理要完善（网络错误、服务器错误都要提示）

---

## 现有相关代码位置（供参考）

- `show(slug)` 函数：约2230行
- `go(slug)` 函数：约2450行
- hashchange监听器：约4815行
- 初始加载：约4820行
- 设备ID获取函数：搜索 `getDeviceId`
- 划线保存：搜索 `HL_KEY_PREFIX` 或 `longchen-hl-`

---

## 交付内容

1. 注册页面的完整HTML/CSS/JS代码
2. 登录页面的完整HTML/CSS/JS代码
3. 用户中心页面的完整HTML/CSS/JS代码
4. 路由处理逻辑（hashchange中识别三个特殊路径）
5. show()函数中添加的渲染逻辑
6. 登录状态检查逻辑（中间件函数）
7. 表单验证逻辑
8. API调用封装函数

---

## 自测清单

- [ ] 访问 #/register 显示注册表单
- [ ] 不填邀请码无法注册
- [ ] 注册成功显示等待审核提示
- [ ] 访问 #/login 显示登录表单
- [ ] 登录成功保存token，跳转到用户中心
- [ ] 未登录访问 #/user-center 跳转到登录页
- [ ] 用户中心显示阅读记录和划线记录
- [ ] 划线记录可以删除
- [ ] 表单验证正常（用户名长度、密码长度、两次密码一致）
- [ ] 暗色模式下样式正常
- [ ] 手机端布局正常
- [ ] 页面切换用go()，返回手势正常
