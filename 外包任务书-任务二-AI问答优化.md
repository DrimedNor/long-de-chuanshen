# 任务二：AI问答体验优化

> 致执行本任务的AI助手：请严格按照以下要求执行。完成后把所有代码改动整理成MD格式返回，不要直接修改任何文件。
> 网站：https://longchen-nyingtik.wiki/
> 技术栈：自研零依赖Python构建器（build_site.py，约5000行），生成单文件HTML SPA，hash路由，无框架。

---

## 通用约束

1. **只修改 build_site.py 一个文件**，不碰其他文件。
2. **所有颜色用现有CSS变量**，禁止硬编码色值。
3. **不要自行添加任何新功能**，只做本任务书列出的修改。
4. **交付格式**：把所有代码改动整理成MD格式，包含：
   - 每个改动的位置（函数名/大约行号）
   - 旧代码 → 新代码的完整对比
   - 改动说明
   - 自测清单

---

## 背景

用户反馈AI搜索存在以下问题：
1. 模糊匹配能力不足（输入"学号救命稻草"搜不到"唯一的救命稻草：选好上师"）
2. 引用来源显示数字[1][2][3]但不列出对应文章
3. AI回复语气过于"佛里佛气"，需要生活化、温暖平和
4. 回复有时显示不全就停止了

现有AI问答架构：
- 前端检索：searchKnowledge 函数，从knowledge.json（全文分段索引）中检索相关文章
- API调用：sendAiAsk 函数，调用 https://ai.longchen-nyingtik.wiki（Cloudflare Workers代理）
- 后端调用：腾讯云TokenHub的DeepSeek API，模型 deepseek-v4-flash-0731
- 知识库：knowledge.json，约11.2MB，包含16109条段落级知识块

---

## 需求一：增强模糊匹配

### 现有问题
当前检索是精确关键词匹配，用户输入错别字或简称时搜不到。

### 改进方案
在现有检索逻辑基础上，增加以下匹配策略（按优先级排序）：

1. **拼音首字母匹配**：
   - 把文章标题和关键词转成拼音首字母
   - 如"xsm"匹配"选上师"，"wsm"匹配"为什么"
   - 用户输入全是字母时启用此匹配

2. **同义词扩展**：
   - 建立修行领域同义词典（至少20组）：
     - 救命=依靠=依怙=皈依
     - 上师=师父=老师=善知识
     - 修行=修持=实修=功夫
     - 无常=变化=不确定=无常观
     - 因果=业力=业报=因果报应
     - 慈悲=仁爱=同情=同理心
     - 智慧=般若=明见=洞察力
     - 福报=功德=资粮=善业
     - 烦恼=痛苦=情绪=困扰
     - 解脱=自在=自由=解脱道
     - 等等...
   - 检索时把用户输入的词替换成同义词一起检索

3. **部分匹配**：
   - 用户输入3个字以上，只要标题或内容包含其中2个字就算匹配
   - 匹配度按包含字数比例计算

4. **错别字容错**：
   - 计算编辑距离（Levenshtein distance）
   - 编辑距离≤2算匹配
   - 只对长度≥3的词启用

### 排序规则
匹配结果按相关度排序：
1. 标题精确匹配（权重最高）
2. 标题部分匹配
3. 内容精确匹配
4. 内容部分匹配
5. 同义词匹配
6. 拼音匹配
7. 错别字容错匹配

---

## 需求二：引用来源优化

### 现有问题
AI回复中引用了[1][2][3]，但用户不知道这些数字对应哪些文章。

### 改进方案
1. 在AI回复末尾，添加"引用来源"区域
2. 列出每个引用数字对应的文章标题
3. 每个引用可点击，点击跳转到对应文章
4. 跳转后AI搜索面板最小化为左上角悬浮按钮（现有功能，调用minimizeSearchPanel()）
5. 引用来源样式：
   ```
   📚 引用来源：
   [1] 文章标题一（可点击跳转）
   [2] 文章标题二（可点击跳转）
   [3] 文章标题三（可点击跳转）
   ```

### 技术实现
- sendAiAsk函数返回结果后，解析回复中的[数字]标记
- 从检索结果中找到对应的文章
- 在回复末尾追加引用来源列表
- 每个引用绑定onclick事件，调用go(文章slug)并最小化搜索面板

---

## 需求三：AI回复语气调整

### 现有问题
AI回复过于"佛里佛气"，开头经常是"阿弥陀佛"、"善哉善哉"，用大量佛经术语。

### 新的system prompt
```
你是龙钦宁提资料库的AI助手，基于本站收集的龙钦宁提相关资料回答问题。

回答风格要求：
1. 用生活化的语言，像一位有智慧的朋友在聊天，不要像高僧在开示
2. 温暖平和，有同理心，给人希望和信心
3. 可以用比喻、故事来说明道理，但不要堆砌佛经术语
4. 不要开头就说"阿弥陀佛"、"善哉"、"施主"之类的话
5. 不要用"诸法无常"、"诸行无我"这种文言句式，改成大白话
6. 回答要有智慧见地，但表达方式要接地气
7. 如果问题涉及修行困惑，先共情再给出角度，不要说教
8. 回答长度适中，不要太长，重点突出

错误示例：
"诸法无常，诸行无我，当勤精进，勿放逸。"

正确示例：
"其实我们每个人都在变化中，今天的你和昨天的你已经不一样了。接受这种变化，反而能让心安定下来。"

回答规则：
- 首先基于本站资料回答，引用资料时标注[1][2][3]
- 如果本站没有相关内容，明确说明"本站暂无相关内容，以下是基于通用知识的回答"
- 不要编造资料中没有的内容
- 涉及具体修行方法时，提醒"具体修行请依止自己的上师指导"
```

### 技术实现
- 修改sendAiAsk函数中的system prompt
- 确保messages数组中system角色的content是新的prompt

---

## 需求四：回复长度控制

### 现有问题
AI回复有时显示不全就停止了。

### 改进方案
1. 检查API调用参数，确保max_tokens足够（至少2000）
2. 如果回复被截断（finish_reason为length），在末尾显示：
   ```
   ⚠️ 回复较长已截断，可细化问题获取完整回答。
   ```
3. 在搜索面板中添加"继续生成"按钮（可选，如果API支持上下文续传）
4. 如果不支持续传，至少提示用户可以换个问法

---

## 现有相关代码位置（供参考）

- searchKnowledge 函数：搜索"function searchKnowledge"
- sendAiAsk 函数：搜索"function sendAiAsk"
- loadKnowledge 函数：搜索"function loadKnowledge"
- minimizeSearchPanel 函数：搜索"function minimizeSearchPanel"
- AI搜索面板HTML：搜索"search-panel"或"aiAsk"
- knowledge.json加载：搜索"knowledge.json"

---

## 交付内容

1. 模糊匹配增强的完整代码（拼音、同义词、部分匹配、错别字容错）
2. 同义词典（至少20组）
3. 引用来源显示的完整代码
4. 新的system prompt
5. 回复截断处理代码
6. 相关CSS样式（引用来源区域的样式）

---

## 自测清单

- [ ] 输入"学号救命稻草"能搜到"唯一的救命稻草：选好上师"
- [ ] 输入"xsm"能搜到"选上师"相关文章
- [ ] 输入错别字（如"无长"）能搜到"无常"相关文章
- [ ] AI回复末尾显示引用来源列表
- [ ] 点击引用来源能跳转到对应文章
- [ ] 跳转后AI搜索面板最小化
- [ ] AI回复语气生活化，不"佛里佛气"
- [ ] 回复被截断时有提示
- [ ] 暗色模式下引用来源样式正常
- [ ] 手机端引用来源布局正常
- [ ] 不影响现有AI问答的基本功能
